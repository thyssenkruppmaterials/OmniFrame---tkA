[build]
builder = "DOCKERFILE"
watchPatterns = ["src/**", "Cargo.toml", "Dockerfile"]

[deploy]
startCommand = "./ai-service"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[[services]]
name = "drone-ai-service"
port = 8001
