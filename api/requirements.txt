# OneBox AI Python API Dependencies

# Web Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
pydantic[email]>=2.5.0
pydantic-settings>=2.1.0

# Database & Supabase
supabase>=2.3.0
httpx>=0.26.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
itsdangerous>=2.1.0

# Caching
cachetools>=5.3.0
redis>=5.0.0

# SAP RFC Integration
# NOTE: pyrfc requires SAP NW RFC SDK to be installed
# See: https://github.com/SAP/PyRFC
# pyrfc is installed separately in Dockerfile from GitHub (PyPI versions are yanked/incompatible)
# pyrfc>=3.3

# Smartsheet Integration 1.0
# Pin below 4.0.0: smartsheet-python-sdk 4.x restructured the package so that
# `from smartsheet import Smartsheet` no longer resolves (import yields a
# namespace package -> "cannot import name 'Smartsheet' from 'smartsheet'
# (unknown location)"), which aborts API startup. 3.7.2 is the known-good line.
smartsheet-python-sdk>=3.0.0,<4.0.0

# Data Processing
pandas>=2.0.0

# Utilities
python-dotenv>=1.0.0
