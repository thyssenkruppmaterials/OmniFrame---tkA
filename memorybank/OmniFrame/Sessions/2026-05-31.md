---
tags: [type/session, status/active, domain/frontend, sidebar, ux]
created: 2026-05-31
---
# Session 2026-05-31

## Done
- Fixed the sidebar pin button floating into the middle of an expanded nav group ("out of whack" — the blue pin appeared next to "Shift Productivity" when "Labor Management" was the pinned/open group).
  - Root cause: `NavPinButton` in `OptimizedNavGroup` was `absolute top-1/2 -translate-y-1/2` relative to `SidebarMenuItem` (`<li>`), but that `<li>` also wraps the expanded `CollapsibleContent` (all sub-items). So the percentage center landed in the vertical middle of the *whole open section*, not on the header row.
  - Fix: anchor to a fixed top inset matching the `h-8` (32px) header button — `top-1.5` (6px), dropping `top-1/2 -translate-y-1/2`. Pin is `size-5` (20px) → (32−20)/2 = 6px centers it on the header whether open or closed. Mirrors shadcn `SidebarMenuAction`.
  - Also widened the touch target on both the nav-group pin and the `OnlineUsersPanel` pin via `after:absolute after:-inset-1.5` (RF/operator devices) without changing the visual size. The `OnlineUsersPanel` pin's `top-1/2 -translate-y-1/2` was left intact — its wrapper only contains the header row, so it was already correct.
- Updated pattern note [[Sidebar-Pin-Lock]] with the vertical-anchor gotcha (don't center on the `<li>` when it wraps collapsible content).

## Verified
- ESLint clean on both modified files (`optimized-nav-group.tsx`, `online-users-panel.tsx`) — no new warnings.
- Pure className + comment changes; no type changes. No tests assert pin positioning.

## Files
- `src/components/layout/optimized-nav-group.tsx` — `NavPinButton` vertical anchor fix + touch target.
- `src/components/presence/online-users-panel.tsx` — pin touch target only (position already correct).


## Done (cont.) — Online panel defaults to collapsed
- `OnlineUsersPanel` now **defaults to collapsed on every load**. The chevron still expands it for the current session, but an expanded state is only remembered across reloads when the panel is **pinned** (pin = "lock my choice"). Unpinned panels always start collapsed.
  - `computeInitialCollapsed(pinnedKey, collapsedKey)` for the `useState` initializer: not authenticated → collapsed; not pinned → collapsed; pinned → restore persisted `panelCollapsed` (default true).
  - Hydration effect rewritten: reads `panelPinned` first, then restores `panelCollapsed` only when pinned, else forces `true`. Dropped the legacy global-key migration read (legacy key is now only the unauthenticated storage fallback).
  - `togglePanelPinned` now captures the current `panelCollapsed` into `collapsedKey` at pin time (mirrors the nav-group pin's "capture current state") so pinning a freshly-collapsed panel doesn't restore a stale persisted expanded state on next load.
  - The sidebar (`AuthenticatedLayout`) persists across route changes, so "every load" effectively means page reload / sign-in — manual expand survives in-session navigation; reload returns to collapsed unless pinned.
- ESLint clean. Pure component-state/logic change; no schema or API impact.


## Done (cont.) — LL01 Warehouse Activity Monitor 500/crash fix
- **Symptom:** Running the Warehouse Activity Monitor query (Inventory Management tab) white-screened the SAP Testing page into the error-boundary "500" screen. Console: `TypeError: Cannot read properties of undefined (reading 'find')` in the `feature-admin-sap` chunk (`HeatmapTab`), inside an `Array.map`. (The `127.0.0.1:8765/health` `ERR_CONNECTION_REFUSED` and notifications `404` lines were unrelated — expected local-agent probe in fleet mode + a separate endpoint miss.)
- **Root cause (shape drift):** Fleet `dispatch` (`use-execution-mode.ts`) unwraps a job row as `{ ok, error, step, ...(finalRow.result ?? {}) }`. A failed / result-less job (offline-agent reclaim, claim-lease watchdog, non-LL01 error body) has `result === null`, so the object lacks `categories`/`plants`/`errors`. `setLl01Result(data)` stored that, and `HeatmapTab`'s `result?.categories.find(...)` threw — `?.` only guards a NULL `result`, not `result.categories` being undefined. Local path has the same gap on a non-2xx error body.
- **Fix:** Normalize at the dispatch boundary in `inventory-management-tab.tsx` into a well-formed `LL01RunResult` (all arrays defaulted) before `setLl01Result`; success gated on `ll01.ok && ll01.categories.length > 0`; failure now also pushes to the SAP console card. Belt-and-braces optional-chaining/`?? []` guards added to `HeatmapTab` + `AgingTab` so future drift renders zeros instead of crashing. Mirrors [[Fix-MapStatistics-Shape-Drift]] ("normalize at the boundary, not every call site").
- **Verified:** New regression test (`HeatmapTab` with a drifted `{ok:false}` result must not throw); `warehouse-activity-monitor.test.tsx` (8) + `aging-tab.test.tsx` (8) all green; ESLint clean on edited files.
- See [[Debug/Fix-LL01-Fleet-Result-Shape-Drift-Crash]].

## Files (cont.)
- `src/features/admin/sap-testing/components/inventory-management-tab.tsx` — boundary normalization of the LL01 dispatch result.
- `src/features/admin/sap-testing/components/HeatmapTab.tsx` — defensive `categories?`/`rows ?? []` guards.
- `src/features/admin/sap-testing/components/AgingTab.tsx` — same defensive guards.
- `src/features/admin/sap-testing/components/__tests__/warehouse-activity-monitor.test.tsx` — drifted-result regression test.


## Done (cont.) — LL01 save & date-picker re-reference (full feature)
- **Ask:** save each Warehouse Activity Monitor run + a date picker next to "Run Query" to view historical data. Brainstormed → decisions: **full fidelity** (historical runs support drill-down + Aging), **keep all** indefinitely, **agent-write** persistence.
- **Why a new table:** `ll01_activity_snapshots` is counts-only; drill-down + Aging need `categories[].rows`, which was never persisted. New `ll01_activity_runs` (migration 333, applied via Supabase MCP; RLS mirrors snapshots; no security advisor flag) stores one JSONB row per run = the exact `LL01RunResult`. Counts table left untouched (Trend keeps using it).
- **Agent:** `_insert_run(...)` posts the full payload alongside the existing counts insert (own try block; `payload_version` hoisted). **EXE rebuild required** — history accrues for new-build runs only; older runs stay counts-only.
- **Frontend:** `useLL01History` hook (slim index + lazy payload, normalized like the live dispatch path), `<LL01HistoryPicker>` (shadcn Calendar popover next to Run Query — only run-days enabled, per-time list for multi-run days, "Current run" reset), tab wiring (`effectiveResult = ll01ViewedRun ?? ll01Result`; completed run snaps back to live + refreshes index), and a "Viewing saved run from {date}" banner in `WarehouseActivityMonitorView` with "Back to current run".
- **Verified:** agent persistence tests (2) + picker tests (4) pass; full sap-testing suite 20 green; `pnpm exec tsc -b` clean; ESLint clean on all edited files.
- See [[Implementations/Implement-LL01-Run-History-Date-Picker]].

## Files (cont. 2)
- `supabase/migrations/333_create_ll01_activity_runs.sql` — full-payload run store (+ applied via MCP).
- `omni_agent/ll01_warehouse_activity_monitor.py` — `_insert_run` full-payload persistence.
- `src/features/admin/sap-testing/hooks/use-ll01-history.ts` — history index + lazy run loader.
- `src/features/admin/sap-testing/components/ll01-history-picker.tsx` — date picker.
- `src/features/admin/sap-testing/components/warehouse-activity-monitor-view.tsx` — historical banner + props.
- `src/features/admin/sap-testing/components/warehouse-activity-monitor-types.ts` — `LL01RunIndexEntry`.
- `src/features/admin/sap-testing/components/inventory-management-tab.tsx` — picker wiring + viewed-run state.
- `omni_agent/tests/test_ll01_run_persistence.py`, `components/__tests__/ll01-history-picker.test.tsx` — tests.


## Done (cont. 3) — Failed run collapsed the heatmap grid
- **Symptom (from screenshot):** after running with an offline/erroring agent, the heatmap kept the 7 category rows but **lost the plant columns** (JSF/JSM/PDC/WH5/WH8) — collapsed to just Category/Trend/Total, all zeros. The earlier crash fix stopped the white-screen but exposed this degraded render.
- **Cause:** boundary normalization sets `plants: []` on a failed run; `HeatmapTab`/`AgingTab` used `result?.plants ?? [...LL01_PLANTS]`, and `??` doesn't catch an empty array → `plants.map` rendered zero columns. An all-zeros grid was also indistinguishable from a genuine "zero issues" run.
- **Fix:** treat empty `plants` as missing → fall back to `LL01_PLANTS` (both tabs); lift the dispatch `error`/`step` into `errors[]` on failure so `HeatmapTab` shows a red "Last run didn't complete — {reason}" banner and `AgingTab` shows a matching failure state (not the "older build" fallback); hardened `result.errors?.[0]?.detail`. Regression test added (failed run keeps columns + shows banner). 21 sap-testing tests green; tsc clean.
- Captured under [[Debug/Fix-LL01-Fleet-Result-Shape-Drift-Crash]] (Follow-up section).


## Done (cont. 4) — Agent EXE rebuild (ships LL01 `_insert_run`)
- Rebuilt the agent EXEs so the LL01 full-fidelity run store (`_insert_run` → `ll01_activity_runs`, migration 333) ships. Built via `MacWindowsBridge/Omni-Agent/build_exe.bat` inside the Parallels Windows VM (`prlctl`), `AGENT_VERSION 2.1.0` (no bump — additive). All three EXEs built clean (exit 0):
  - `OmniFrame_Agent.exe` (worker) — `13988260b44421f857e52f85081b52d40b627d60fcea45041762fddf1f30d886`
  - `OmniFrame_AgentMaster.exe` — `75dbd4c807825ac8bc6afbb5edfd6da44c3265281d75f034c8df11474737ad75`
  - `OmniFrame_Connect.exe` — `def6a036afbfeec188f0b4051332cc10fcfa2b160490b9882a3a795e89d773e1`
  - Output: `dist/` (+ `.sha256` sidecars, `OmniFrame_AgentMaster.zip`, `OmniFrame_Connect.zip`).
- **Gotcha for future me:** `prlctl exec` runs as `nt authority\system`, which (via macOS TCC) can see `\\Mac\Home\Downloads` but **not** `\\Mac\Home\Documents` — so `build_exe.bat`'s default `OMNIFRAME_AGENT_SOURCE` (workspace under Documents) is invisible and `dir \\Mac\Home` *hangs*. Workaround used: `rsync` the workspace `omni_agent/` → `~/Downloads/MacWindowsBridge/_agent_src` (Downloads is SYSTEM-readable), point `OMNIFRAME_AGENT_SOURCE` at it via a small wrapper `.bat` that also does `call build_exe.bat < nul` (so the script's `pause` calls don't block non-interactive exec). Staging + wrapper deleted after the build. Alternatively, run `build_exe.bat` from the interactive `prlctl enter` session (that user *can* see Documents).
- The harmless `ERROR: Hidden import 'pywin32' not found` in the Connect build is pre-existing (the real `win32*` submodules load via actual imports); Connect still built.
- **Deploy step remaining (user/operator):** distribute `dist/OmniFrame_AgentMaster.zip` (or `OmniFrame_Connect.zip`) to the Citrix box and restart the agent so the new worker EXE runs. Full LL01 history begins accruing on runs by this build; pre-rebuild runs stay counts-only in Trend.


## Done (cont. 5) — LL01 killed by 120s stuck-job watchdog (fix + rebuild)
- **Symptom:** LL01 job failed with `Watchdog: job exceeded 120s timeout — likely SAP session hang`, but the report legitimately runs ~5 min. The agent's `stuck-job-watchdog` uses **absolute time since claim** with a flat 120s cap for all endpoints, so LL01 (and LX25's fan-out) mis-fire. (Server-side lease stays fresh via the 30s heartbeat, so the agent-local watchdog was the sole killer. `progress_at` can't be used — the heartbeat bumps it unconditionally, so it tracks liveness not SAP work.)
- **Fix:** per-endpoint watchdog budget in `agent.py` — `_WATCHDOG_ENDPOINT_TIMEOUT_SEC` (`/sap/ll01/warehouse-activity`=900s matching the FE 15-min dispatch ceiling, `/sap/lx25/inventory-completion`=600s) + `_watchdog_timeout_for(endpoint)` = `max(per-endpoint, env default)`. New `state.active_job_endpoint` set on claim, cleared at every active-job clear site; watchdog reads it. Everything else keeps the fast 120s default so genuine COM hangs still recover quickly.
- **Verified:** `py_compile` clean; live in VM (py3.14) `_watchdog_timeout_for` → LL01 900 / LX25 600 / query 120 / None 120; unit test `tests/test_watchdog_endpoint_budget.py` (skips on Mac py3.9). Rebuilt worker EXE (new hash `bd9bf8241a02b42db94aeefb9d7e9d8b1716632d4fc222801638a14f91e561db`; Master/Connect unchanged at `75dbd4c8…`/`def6a036…`).
- This watchdog fix is a **prerequisite** for the LL01 history feature — runs were dying at 120s before reaching `_insert_run`, so nothing was persisting.
- See [[Debug/Fix-LL01-Watchdog-120s-Timeout]]. **Redeploy + restart the agent on Citrix to take effect** (worker EXE rebuilt this turn; same `dist/` bundles).
- Files: `omni_agent/agent.py` (watchdog budget), `omni_agent/tests/test_watchdog_endpoint_budget.py` (new test).


## Done (cont. 6) — Fleet-mode LL01 progress bar (FE-only)
- **Symptom:** in FLEET mode the LL01 progress bar froze at "Fetching all 5 plants…" — no movement as warehouses (plants) completed. (Local mode polls the agent's `/sap/ll01/warehouse-activity/progress` directly; fleet mode disables that poll because the Citrix agent isn't browser-reachable.)
- **Root cause:** `WarehouseActivityMonitorView`'s poll effect early-returns when `executionModeIsFleet`, and `ll01Progress` is only set once (static) at run start → frozen.
- **Fix (no agent/rust change):** the agent already relays stdout to the browser via `WsEvent::SapAgentConsoleLine` (org-scoped work-service WS), and the LL01 worker prints `[ll01] Plant X/Y: PLANT` per plant. New `useLL01FleetProgress(enabled)` hook taps that stream and parses those lines into `LL01Progress`; the view uses `effectiveProgress = fleetProgress ?? pollProgress ?? progress`. Pure parser `parseLL01PlantLine` lives in `warehouse-activity-monitor-types.ts` (dependency-free, so its unit test doesn't drag in the Supabase auth client). Granularity = per plant (5 ticks) which exactly matches "as warehouses get completed". Tolerant parse: unrecognised lines are ignored (degrades gracefully if the agent print format changes).
- **Verified:** 6 parser unit tests; full sap-testing suite 27 green; `tsc -b` clean; ESLint clean. **FE-only — no agent rebuild / rust deploy needed; ships with the next frontend deploy.**
- Files: `src/features/admin/sap-testing/hooks/use-ll01-fleet-progress.ts` (new), `…/components/warehouse-activity-monitor-view.tsx` (wire), `…/components/warehouse-activity-monitor-types.ts` (`parseLL01PlantLine`), `…/hooks/__tests__/use-ll01-fleet-progress.test.ts` (new).
- Note: per-plant cadence (worker prints once per plant). Finer per-category progress would need an agent change (extra console line per category) — deferred unless requested.


## Done (cont. 7) — LL01 "data didn't make it back" (job reaped after data persisted)
- **Symptom:** fleet LL01 showed `watchdog: max_attempts (1) exhausted with lease expiry`; user said the job completed in SAP/Citrix but data didn't come back.
- **Diagnosis (live DB):** the watchdog rebuild DID deploy (13:39 run = old "120s" message; 14:16 + 15:11 = lease-expiry reaper). `sap_agent_jobs` latest LL01 = `failed`/`watchdog_max_attempts`/`has_result=false`, last heartbeat 15:17:18 → lease 15:18:48 → reaper 15:19:06. **BUT `ll01_activity_runs` has the run `ok=true`, 7 cats, 5 plants, 350s** — the agent finished + persisted; the server-side `claim_sap_agent_job` zombie-reaper (migration 291) flipped the row to failed as the 90s lease lapsed, racing `jobs_complete`. Data was safe; FE just showed the failed job.
- **Fix (FE-only):** FE now mints `snapshot_run_id` and passes it in the dispatch (agent already honours it); new `recoverLl01FromHistory(id)` loads the persisted `ll01_activity_runs` row via `useLL01History.loadRun` when the run comes back failed/empty (failed-result branch AND catch), shows it with a "recovered" toast, else falls through to the normal failure. The run also shows in the History picker regardless.
- **Verified:** DB-confirmed persisted run; tsc + 27 sap-testing tests green; ESLint clean. **FE-only — no rebuild.**
- **Recommended follow-up (agent):** raise the heartbeat lease (`_bump_current_job_lease` `lease_seconds` 90 → ~300) so the reaper stops firing on healthy long runs (needs a rebuild). The FE recovery is defense-in-depth regardless.
- See [[Debug/Fix-LL01-Job-Reaped-But-Data-Persisted]].
- Files: `src/features/admin/sap-testing/components/inventory-management-tab.tsx` (mint id + `recoverLl01FromHistory` + failed/catch recovery).


## Done (cont. 8) — History button never worked + lease change + rebuild
Three findings from reviewing the live browser:
- **Browser "stuck"** = the recovery + fleet-progress fixes (and the history fix below) aren't deployed yet (live build is 15:43Z / 10:43 AM). They're verified-correct in the workspace; **need a frontend deploy**.
- **History button stuck disabled (real FE bug, root-caused via browser+DB):** `useLL01History`'s `untypedFrom()` returned the DETACHED `supabase.from`, then called it standalone → `this===undefined` → throws inside supabase-js → `refreshIndex`/`loadRun` silently rejected (try/finally, no catch) → 0 runs → button disabled (and last turn's recovery never fired). Proven the data path is fine: a raw REST probe from the browser with the exact hook shape returned 200 + 4 rows; user's org == runs' org (`c9d89a74…`). Fix: call `supabase.from(table)` inline to preserve `this` (mirrors the working `fetchSnapshots`). FE-only. See [[Debug/Fix-LL01-History-Detached-Supabase-From]].
- **Lease change + rebuild (root-cause fix for the reaper):** per-endpoint heartbeat lease — `_lease_seconds_for(endpoint)` (LL01/LX25 600s, default 90s) in `_bump_current_job_lease`; rust already honours request `lease_seconds`, so agent-only. Keeps the job row claimed through completion so the migration-291 zombie reaper stops failing healthy long runs. Verified live in VM (LL01=600/default=90); **agent rebuilt — worker hash `a63ee35ce1402c235c13b05a47721d071f25b58ad31444fd58a2b14fb9716105`** (Master/Connect unchanged). See [[Debug/Fix-LL01-Job-Reaped-But-Data-Persisted]] (root-cause fix section).
- **Verified:** agent `py_compile` + VM import; `_lease_seconds_for` test added; FE tsc + 27 sap-testing tests green; ESLint clean.
- **Deploy needed:** (1) frontend deploy for the History `this`-fix + recovery + fleet-progress; (2) redeploy the rebuilt worker EXE to Citrix for the lease change.
- Files: `omni_agent/agent.py` (`_lease_seconds_for` + per-endpoint lease), `omni_agent/tests/test_watchdog_endpoint_budget.py` (lease test), `src/features/admin/sap-testing/hooks/use-ll01-history.ts` (`this`-binding fix).


## Done (cont. 9) — OmniBelt review: flash-on-load fix + perf passes
- **Ask:** comprehensive OmniBelt review — make it faster / zero-latency motion, and fix "it's turned off but flashes on first page load then disappears."
- **Flash fix (the explicit bug):** `useOmnibeltVisibility` Layer 5 only hid on an *explicit* `orgEnabled === false`; while the org kill-switch query was in flight (`undefined`) it fell through to **visible**, so a disabled org painted the launcher then unmounted it when the query resolved false — the flash. Switched to **fail-closed** (`orgEnabled !== true` → hidden) + a `localStorage` last-known seed via `placeholderData` so confirmed-enabled orgs still paint on the first frame (no flash, no fade-in) and disabled orgs never paint. Uncovered a latent test bug: the "loading" test passed `setup({ orgEnabled: undefined })`, but the helper's destructuring default coerced it back to `true`, so the fail-open path was never actually tested — now drives the `useQuery` mock directly. See [[Debug/Fix-OmniBelt-Disabled-Flash-On-Cold-Load]].
- **Perf — gate side-effecting hooks behind visibility:** moved `useOmnibeltJobs` (WS job aggregator), `useOmnibeltConfigInvalidator` (WS handler) and `useOmnibeltKeyboard` (global keydown) into a new `<OmniBeltActiveSurface />` that mounts ONLY when visible. Previously they ran on every authenticated page and self-gated, so a disabled/route-excluded/native user still opened a `workServiceWs` connection + window listener for chrome they never see. ⌘B only toggles `collapseState` (never un-hides), so gating the keyboard is safe.
- **Zero-latency first paint:** default skin `skystrip` is now a static import in `SKIN_REGISTRY` (was `React.lazy`), so the resting chrome paints on the first frame instead of after a lazy-chunk round-trip + Suspense gap. It still emits its own `feature-omnibelt-skin-skystrip` chunk (vite manualChunks unchanged) — only its load timing moves earlier (preloaded in parallel with `feature-omnibelt`). Alternate skins (`pill`, `orb`) stay lazy.
- **Verified:** 264 omnibelt unit tests green; `tsc -b` clean; ESLint clean on the 3 changed files. FE-only — no agent/rust/migration. Ships with next frontend deploy.
- **Recommended (not yet done, presented in review):** make `willChange` conditional (drag/animation only) on Pill + SkyStrip instead of permanently promoting a compositor layer; dedupe the org kill-switch double-fetch (visibility hook reads Supabase directly while bootstrap also returns `kill_switch.enabled`); consider folding skystrip into the `feature-omnibelt` chunk after a bundle-budget check.
- Files: `src/features/omnibelt/hooks/useOmnibeltVisibility.ts`, `src/features/omnibelt/OmniBeltHost.tsx`, `src/features/omnibelt/__tests__/useOmnibeltVisibility.test.tsx`.


## Done (cont. 10) — OmniBelt review: worked all 4 remaining findings + full-build validation
- **#1 `will-change` (surgical promotion):** every OmniBelt motion surface hard-coded `style={{ willChange: 'transform' }}` permanently, promoting a compositor layer at mount and never releasing it — the anti-pattern flagged by the motion-performance checklist (rule 6: temporary & surgical; avoid many promoted layers). Verified in the installed `framer-motion@12` source that auto will-change is gated behind `MotionGlobalConfig.WillChange`, which this app never sets — so simply *removing* the hint would give NO promotion even during the morph. Correct fix = framer's own `useWillChange()` motion value (it stays `auto` at rest and flips to `transform`/`opacity` only while a value animates). Converted all 9 call sites across 8 files: `OmniBeltPill`, `OmniBeltSkyStrip` (strip + bloomed panel), `ToolTile` (the "many tiles = many layers" case), `OmniBeltPanel`, `PillEdgeNub`, `PillMiniOrb`, `OrbShellPopover`, `AgentChatDialog`.
- **#2 kill-switch double-fetch:** intentionally left as-is and documented WHY in `useOmnibeltVisibility` — the visibility hook reads `settings` straight from Supabase (reachable when the FastAPI/Rust bootstrap is circuit-broken), whereas `useOmnibeltBootstrap`'s `kill_switch.enabled` is fail-OPEN in its offline placeholder. Consolidating would couple the kill switch to FastAPI uptime and lose the loading/enabled distinction. Added a "do NOT consolidate" comment so it isn't re-broken.
- **#3 halo re-renders:** reviewed, no change needed — `useOmnibeltJobs` already applies a 1% progress-diff threshold + terminal-hold eviction, so a chatty backend can't trigger a halo re-render storm.
- **#4 stale vite chunk:** `vite.config.ts` manualChunks comment still claimed "host + Pill skin (default)" and split `skystrip` into its own lazy chunk — stale since skystrip became the default + got the static (eager) import. Removed the `skin-skystrip` rule so skystrip folds into the always-resident `feature-omnibelt` slice (a separate chunk for always-loaded bytes was just an extra parallel request); kept `orb` lazy/separate. Left `pill` + shared `PillEdgeNub`/`PillMiniOrb` in `feature-omnibelt` (shared across skins — a separate chunk would split shared modules).
- **Validation:** `tsc -b` clean · ESLint clean on `src/features/omnibelt` + `vite.config.ts` · 428 omnibelt unit tests pass (29 files; the lone non-zero exit is the pre-existing `storage.getItem` unhandled rejection in `useOmnibeltConfigInvalidator.test.tsx`, proven on stashed baseline) · `pnpm build` exit 0 · dist confirms `feature-omnibelt` (skystrip folded) + separate `feature-omnibelt-skin-orb`, no standalone skystrip chunk.
- **Pre-existing finding (NOT mine, flagged for the team):** the repo bundle budget already FAILS on a clean `main` build — total **10473.6 KB** vs the 7500 KB hard gate, with 3 oversized chunks (`warehouse-location-map`, `feature-admin`, `feature-rf-interface`). Proven by stashing my changes and rebuilding (identical failure). My changes are net **−0.12 KB** on total (folding skystrip removed per-chunk overhead). So `quality:check`/`quality:ci`'s bundle gate is currently red on main independent of OmniBelt — worth a separate code-split pass on those three features.
- Files: `src/features/omnibelt/skins/pill/{OmniBeltPill,PillEdgeNub,PillMiniOrb}.tsx`, `src/features/omnibelt/skins/skystrip/OmniBeltSkyStrip.tsx`, `src/features/omnibelt/skins/orb/OrbShellPopover.tsx`, `src/features/omnibelt/panel/{OmniBeltPanel,ToolTile}.tsx`, `src/features/omnibelt/tools/shells/agent-chat/AgentChatDialog.tsx`, `src/features/omnibelt/hooks/useOmnibeltVisibility.ts`, `vite.config.ts`. See [[Debug/Fix-OmniBelt-Disabled-Flash-On-Cold-Load]].
