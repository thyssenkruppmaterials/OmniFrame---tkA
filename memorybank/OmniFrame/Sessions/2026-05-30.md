---
tags: [type/session, status/active]
created: 2026-05-30
---
# Session — 2026-05-30 — Rust services review, prod fixes, mimalloc+moka

## Summary
Reviewed the 6 Rust services for a dependency-modernization initiative, fixed three production issues surfaced by the Railway logs, then shipped the first crate increment (mimalloc + moka) on `rust-work-service` with full tests, and cleaned up the resulting build warnings.

## Production fixes → [[Fix-Work-Service-Monorepo-Root-Directory-Misdeploy-2026-05-30]]
- **work-service was serving ~88% errors** because its Railway service had **no Root Directory** → built the repo-root Python/FastAPI app instead of the Rust binary. Fixed by setting Root Directory = `rust-work-service` (user) + redeploy. Verified Rust binary live (`/health` 0.1.42, `/ws` up, PgListeners + presence working, org `c9d89a74` back on the Rust WS).
- **s2s API key**: `onbx_wk_service_key_2026` was never registered → provisioned a real `onbx_wk_` row in `service_api_keys` (perms `auth:validate`/`auth:permissions`) + set `RUST_CORE_API_KEY`. Verified `Authenticated via service API key` + `last_used_at` populated.
- **rust-core `RUST_LOG`** `info,rust_core_service=debug` → `info` (per-request DEBUG spam gone).
- Flagged (not actioned): exposed plaintext secrets (service-role key etc.) on work-service — needs coordinated rotation.

## Crate review → [[ADR-Rust-Dependency-Modernization-Roadmap]]
7 crates × 6 services, per-crate verdicts + sequencing + constraints. Extends the prior [[Review-rust-dashboard-service-And-Consumer-Integration-utoipa]].

## Implementation → [[Implement-Mimalloc-And-Moka-Auth-Cache-Work-Service]]
- Branch `feat/rust-work-mimalloc-moka-auth-cache`, `rust-work-service` `0.1.43 → 0.1.44`.
- **mimalloc** global allocator (main.rs). **moka** JWT-validation cache in `AuthClient` (30s TTL, `AUTH_CACHE_TTL_SECS`/`AUTH_CACHE_MAX_CAPACITY`, only successes cached, `work_auth_cache_total{outcome}` metric).
- 4 new tests + 178 unit pass; release LTO build clean; no new clippy.
- **Live result: ~85% cache hit rate** (`hit 11 / miss 2`) immediately post-deploy on 0.1.44; rust-core auth load dropped.

## Dead-code cleanup
The 7 build warnings were all in `observability/middleware.rs` — **intentional Phase-1.5 idempotency foundation** (migration 256, runbook cites `cleanup_expired`, `tests/idempotency.rs`), NOT junk. Annotated `#[allow(dead_code)]` with rationale instead of deleting; same for `tests/common/mod.rs`. Build is now warning-free.

## Open items / next session
- **Commit + open PR** for `feat/rust-work-mimalloc-moka-auth-cache` (currently uncommitted; the live 0.1.44 deploy already includes it).
- **Rotate exposed secrets** (service-role key, JWT secret, Redis pw, Smartsheet token) — coordinated pass.
- Next crate increments (per the roadmap): **moka** on rust-core RBAC (currently **unbounded** cache) + rust-streaming session; **utoipa** on rust-core → typed FE client; **rust_decimal** on the cycle-count write path; file the **opentelemetry ADR** before adopting OTLP.

## Links
- [[Optimize-Auth-Cache-Arc-Value-Work-Service]] — Arc-wrapped the auth-cache value end-to-end (cache → middleware → extensions → 12 route files); collapses 2 deep clones/req into pointer bumps. `0.1.44 → 0.1.45`, tests/clippy/release clean.
- [[Fix-Work-Service-Monorepo-Root-Directory-Misdeploy-2026-05-30]]
- [[ADR-Rust-Dependency-Modernization-Roadmap]]
- [[Implement-Mimalloc-And-Moka-Auth-Cache-Work-Service]]
- [[Rust-Work-Service]] · [[ADR-Rust-Work-Service-Availability-SLO]] · [[Roadmap-Rust-WS-Unlocks]]
