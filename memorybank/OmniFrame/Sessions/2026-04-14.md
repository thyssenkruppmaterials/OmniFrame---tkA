---
tags: [type/session, status/active]
created: 2026-04-14
---
# Session 2026-04-14
## Done
- Sidebar: removed Tasks, Business Applications section, placeholder nav (Intelligence Hub; Facility IT Services & Vendor Management). Code: `src/components/layout/data/sidebar-data.ts`.
- Vault: [[Prune-Sidebar-Nav-Placeholders]], updated [[Layout - App Shell]].
## Related
- [[Prune-Sidebar-Nav-Placeholders]]


## Cycle Count Variance Status Fix
- **Issue**: Counts with non-zero variance showing `completed` instead of `variance_review` due to threshold-gated trigger logic
- **Fix**: Migration 215 — any non-zero variance on completion → `variance_review`; `requires_recount` remains threshold-based
- **Data fix**: 127 existing rows corrected
- **Notes**: [[Fix-CycleCount-Completed-With-Variance]]


## Omni-Bridge: LT12 TO Confirmation Automation
- Renamed `onebox_sap_bridge/` folder to `omni_bridge/` and updated build artifacts from OneBox to OmniFrame
- Added LT12 Transfer Order confirmation automation (v2.0):
  - Python: `confirm_transfer_order()`, `_update_putaway_status()`, `_log_to_transaction()`
  - JS: page detection (polls for TO Number + Warehouse table headers), table scanner, batch confirm loop with auto-pagination
  - Bar UI switches between PGI mode (outbound) and TO Confirm mode (Putaway Log Search)
  - After SAP confirmation, PATCHes `rf_putaway_operations` in Supabase so realtime updates flow to the web app
- **Notes**: [[Omni-Bridge - SAP Bridge]], [[Implement-LT12-TO-Confirmation]]


## Cycle Count Reassignment Tracking
- **Feature**: Automatic tracking of reassignment history with previous counter/quantity preservation
- **DB**: Migration 216 — `cycle_count_assignment_history` table + `reassignment_count` column + BEFORE trigger
- **Frontend**: Amber reassignment badge in table + Assignment History card in detail modal
- **Notes**: [[Implement-CycleCount-Reassignment-Tracking]]


## Bulk Import Count Type Prompt
- **Change**: "Import Bulk Counts" now opens a dialog to select count type before importing
- **Flow**: User picks count type → clicks "Import from Clipboard" → count type applied as default to all rows (CSV column overrides if present)
- **Files**: `cycle-count.service.ts` (added `defaultCountType` param), `use-cycle-count-operations.ts` (pass-through), `manual-counts-search.tsx` (dialog UI)