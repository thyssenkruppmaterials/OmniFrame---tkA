---
tags: [type/context, status/active]
created: 2026-05-04
---

# Implementations Index

MOC for the [[Implementations/]] folder — "how features were built". Use [[Patterns/]] for the *what / why*; this index is the *what / where*.

## Cycle counts

- [[Implement-Work-Distribution-Now-Bundle-2026-05-18]] — 2026-05-18 — NOW horizon of [[ADR-Work-Distribution-Pipeline-Architecture-Review-2026-05-18]]. Eight items: migration 311 idle-aware escalator + per-org filter, scheduler per-org reapers (T-2/T-5), Phase 0 generic zone filter (T-4), `WsEvent::ClaimBlockedByZone` canary + admin ribbon (T-3), operator Pull-Next "Resume / Release" affordance + Confirm-chip promotion (T-7), post-deploy smoke claim CI (M-9), stuck-zone runbook refresh (O-1). cargo + tsc + vitest green; pre-existing bundle-budget breach surfaced but not introduced.
- [[Inventory-Counts-Tab-Comprehensive-Redesign]] — dashboard layout pass.
- [[Add-Bulk-Import-Progress-Dialog-Inventory-Counts]] — 2026-05-19 — non-dismissable progress modal + `beforeunload` guard wrapping the client-side row-by-row clipboard import on the Inventory Counts tab; subscribes to `importProgress` already emitted by `useCycleCountOperations`.
- [[Redesign-Manual-Counts-Tab-UI]] — prior styling round.
- [[Manual-Counts-Column-Filters]] — inline filter row.
- [[Add-New-Count-Workflow-Button]] — entry point for new counts.
- [[Wire-Cycle-Count-Workflow-To-RF-Counter]] — RF counter wiring.
- [[Implement-CycleCount-Reassignment-Tracking]] — reassignment audit trail.
- [[Cycle-Count-Bug-Fix-Pass-Migration-252]] / [[Cycle-Count-Final-Hardening-Pass-Migration-253]] — hardening passes.
- [[Cycle-Count-Priority-Rules-And-Heartbeat-Release]] — priority + heartbeat.
- [[Cycle-Count-Zone-Exclusivity]] / [[Cycle-Count-Zone-Sticky-And-Assignments]] — zone engine.
- [[Fix-RF-Cycle-Count-Stuck-Waiting]] — 2026-05-14 — Phase 0 already-assigned bypass + v0.1.41/v0.1.42 lazy-pool fixes; resolved.
- [[Fix-RF-Cycle-Count-Zone-Soft-Reservation-Cascade-2026-05-18]] — 2026-05-18 — single stale `pending+assigned` row in zone `RP` cascades to lock entire team via the Phase 2 zone-mutual-exclusion `soft-released reservation` predicate (`queries.rs:443-462`); ops unblock via admin Unassign on `CC-20260424-1312`; follow-up ADR proposed on tightening the `pending` branch.
- [[Path-Engine-Configuration]] — path rules.
- [[Track-Defer-By-User-History]] — defer history view + dashboard surfaces (this pass).
- [[Part-Number-Verification-Workflow-Step]] — part check step.
- [[Redesign-View-Count-Dialog]] — EditCountModal redesign.
- [[Count-Settings-Enterprise-Redesign]] — Count Settings tab restyled to the unified-workbench shell.

## Work engine

- [[Implement-Work-Engine-Foundation]] / [[Implement-Work-Engine-Operational-Hardening]].
- [[Implement-Picking-WorkType-Follow-On]] / [[Implement-Zoning-Work-Type-Follow-On]].
- [[Wire-Extra-Workflow-Steps-And-Rust-Passthrough]].

## SAP / agent / bridge

- [[Implement-Omni-Agent]].
- [[Implement-Agent-Triggers]] / [[Implement-Agent-Triggers-Control-Center-Round3]].
- [[Implement-Agent-Direct-Realtime]] / [[Implement-Agent-DB-Load-Reduction]] / [[Implement-Frontend-Supabase-Load-Reduction]].
- [[Implement-Agent-SAP-AutoConnect]] / [[Implement-Agent-Supabase-Login-UI]].
- [[Implement-Multi-Agent-Coordination]] / [[Implement-Fleet-Aware-SmartImportButton]].
- [[Implement-SAP-Session-Pinning]] / [[Implement-SAP-Testing-Layout-Polish]].
- [[Implement-LT12-TO-Confirmation]] / [[Implement-LT22-Outbound-Import]].
- [[Implement-Reversal-Rollback-Engine]].
- [[Implement-Material-Master-Dry-Run]].
- [[Implement-One-Click-Ship]].
- [[Implement-Self-Recording-Mode]].
- [[Implement-TO-History-Tab]] — *superseded 2026-05-09 by [[Implement-LT24-History-Trail]]*.
- [[Implement-LT24-History-Trail]] — migrate the standalone TO History admin tab into the **Inventory Management Query Library** (WAREHOUSE) as `lt24-history`. Replaces the flat-table view with `<TransferOrderHistoryView />` — dual-mode visualisation (Journey card view + day-clustered Timeline view) on the same LT24 dataset, 4-card stat ribbon, multi-axis filter chips, Detail Drawer, virtualised journey list, friendly-headers CSV export. Zero agent change (the existing `handler_lt24` already accepts the simple `{material, warehouse, to_number}` shape and routes through `/sap/query`). Bundle win: dropped `react-force-graph-2d` from the `feature-admin-sap` chunk (455.93 KB → 368.64 KB).
- [[Implement-LX25-Inventory-Completion]] — new **Inventory Completion** entry under the **Inventory Management Query Library** (WAREHOUSE). One click runs LX25 ("Inventory Status — List with Totals") sequentially across 5 warehouses (WH5/WH8/JSM/JSF/PDC), each with its dedicated SAP variant (TKAWH5/TKAWH8/TKAJSM/TKAJSF/TKAPDC), and aggregates the per-storage-type cycle-count metrics into an aggregate stat card + 5 per-warehouse cards + sortable detail table. New agent endpoint `POST /sap/lx25/inventory-completion` (capability `lx25-inventory-completion`) implementing the 5-warehouse fan-out in a single SAP COM session loop with a custom text-export parser (the agent's generic multi-format parser would mis-classify the storage-type-marker rows). Per-warehouse failures captured inline so a missing variant doesn't abort the rest of the fan-out. Custom result renderer `<InventoryCompletionView />` uses [[Patterns/Elevated-KPI-Stat-Cards]]. Pydantic model registered in `_JOB_ENDPOINT_MODELS` so fleet-mode dispatch builds the model from the JSON body. AGENT_VERSION stays at 2.0.0 — additive capability, no version bump.
- [[Implement-LL01-Warehouse-Activity-Monitor]] — 2026-05-22 — **Warehouse Activity Monitor** (LL01) sibling under the same WAREHOUSE Query Library group. Agent `POST /sap/ll01/warehouse-activity` (capability `ll01-warehouse-activity-monitor`) runs 5 plants × 7 categories sequentially, persists counts to `ll01_activity_snapshots`, returns row detail in HTTP response. FE `<WarehouseActivityMonitorView />` Heatmap + Trend tabs (Recharts). `database.types.ts` regen deferred. **2026-05-27 follow-up:** added an **Aging** tab (warehouse / quarter / user breakdowns) — see [[LL01-Aging-Breakdown]]. Worker now extracts `created_on` (+ `created_by` on Open TO) for the three transactional categories that previously dropped them; envelope advertises `payload_version: 2`. No EXE rebuild this turn — change ships with the next build.
- [[Implement-LL01-Warehouse-Activity-Report]] — 2026-06-01 — **Generate Report** dropdown (PDF + Excel) for the Warehouse Activity Monitor: Plant × Category heatmap + every category's aging breakdown (Plant × >30/>60/>90, quarter spread, top users), no trend. PDF = print-styled HTML via hidden iframe ("Save as PDF", zero new deps); Excel = styled `.xlsx` via lazy `exceljs`. Shared pure `buildLL01ReportModel`; renders the displayed run (live or saved). Bundle-neutral. FE-only.
- [[Implement-LL01-Run-History-Date-Picker]] — 2026-05-31 — **Save & re-reference** follow-up to the Warehouse Activity Monitor. New `ll01_activity_runs` table (migration 333) persists the FULL run payload (one JSONB row = the exact `LL01RunResult`, incl. `categories[].rows`) indefinitely — enough to reload a past run with drill-down + Aging, unlike the counts-only `ll01_activity_snapshots`. Agent `_insert_run(...)` writes it alongside the counts insert (**EXE rebuild required**; older runs stay counts-only). FE: `useLL01History` (slim index + lazy payload load), `<LL01HistoryPicker>` (calendar popover next to Run Query — only days with runs enabled, per-time list for multi-run days, "Current run" reset), `WarehouseActivityMonitorView` "Viewing saved run" banner. Decisions: full fidelity + keep-all + agent-write. See [[Implement-LL01-Run-History-Date-Picker]].
- [[Implement-Scheduled-Recurring-Jobs]].
- [[Replace-LX03-with-LT10-Bin-Stock-Handler]].
- [[SAP-Audit-Trail]] / [[Job-Queue-Architecture]] / [[Bulk-Export-via-pc]].

- [[Implement-OmniAgent-V2-Tauri-GUI]] -- 2026-05-15 -- Tauri 2 master GUI shell (`omni_agent_v2/gui/` + `crates/agent-gui/`); supervises 6 SAP sessions, streams console output, dispatches quick actions, surfaces fleet + WS telemetry.
- [[Implement-Phase-A-Worker-Hardening]] — 2026-05-21 — Phase A of [[Plan-Multi-Session-Agent-Master]]: additive worker hardening in `omni_agent/agent.py` v2.1.0 (`/admin/ws/reconnect`, `/admin/job/abort`, `/admin/sap/reattach`, extended `/health`, boot env vars for port/identity/SAP indices). Master GUI (Phase B) builds on these probes.
- [[Implement-Phase-B-Master-GUI-Skeleton]] — 2026-05-21 — Phase B of [[Plan-Multi-Session-Agent-Master]]: CustomTkinter master shell (`omni_agent/master/`) — config loader, 3-column tile grid (1–12 workers), `/health` probe loop, `Popen` supervisor with Phase A env injection, stubs for Fix/console/settings. Unblocks Phase D (Fix FSM) and Phase C (console drain).
- [[Implement-Phase-D-Fix-State-Machine]] — 2026-05-21 — Phase D rollup: Section 5 fix FSM end-to-end (D1 tree → D2 Option α admin token + `AdminClient` → D3 network diag → D4 dialogs → D5 tile/master_gui/supervisor wiring). 87 pytest passed in `omni_agent/master/tests/`.
- [[Implement-Phase-C-Console-Streaming]] — 2026-05-21 — Phase C of [[Plan-Multi-Session-Agent-Master]]: per-worker ring buffer (10k), stdout/stderr reader threads, fanout → GUI + log rotation (`W<N>-YYYY-MM-DD.log`, 10 MB / UTC midnight, 7-day retention), `ConsoleDrawerWidget` + live pop-outs, tile `[C]` switches drawer. 112 pytest passed in `omni_agent/master/tests/`.
- [[Implement-Phase-E-Setup-Wizard]] — 2026-05-21 — Phase E of [[Plan-Multi-Session-Agent-Master]]: six-step resumable Setup Wizard (`sap_probe`, `wizard_state`, `wizard_steps/*`), blocks master until `master_config.yaml` + per-worker `agent_service_key.txt`; `write_master_config()` first-run persist; Settings stub `[Re-run Setup Wizard]`. 137 pytest passed in `omni_agent/master/tests/`.
- [[Implement-Phase-F-Persistence-Orphan-Adoption]] — 2026-05-21 — Phase F of [[Plan-Multi-Session-Agent-Master]]: config persistence (`settings_logic`, `MasterSettingsDialog`), hot-apply vs restart-required diffing, workers decrement keep/delete policy, orphan adoption on master launch (`orphan_adoption` + supervisor `register_adopted`), tile **ADOPTED** badge + disabled console until restart, right-click context menu. Sub-notes [[Implement-Phase-F2-Orphan-Adoption]], [[Implement-Phase-F3-Settings-Context-Menu]]. 175 pytest passed in `omni_agent/master/tests/`.
- [[Implement-Service-Key-Strict-Toggle]] — 2026-05-21 — `master.require_service_keys` bool (default `true`): wizard Step 4 skip checkbox, Settings Master globals checkbox, conditional supervisor env injection, restart-required diff. Soft mode allows Supabase session fallback until keys registered.
- [[Implement-Phase-G-Packaging-DualExe]] — 2026-05-21 — Phase G of [[Plan-Multi-Session-Agent-Master]]: dual PyInstaller EXE from `build_exe.bat` (`OmniFrame_Agent.exe` unchanged + new `OmniFrame_AgentMaster.exe`), SHA-256 sidecars, `OmniFrame_AgentMaster.zip`, `master/__main__.py` + CLI `--version`/`--probe-only`, Windows `smoke_check_master_exe.ps1`, operator README under `master/build/`. 186 pytest passed in `omni_agent/master/tests/` (macOS static; Windows build + smoke manual).
- [[Implement-Phase-D3-Network-Diagnostic]] — 2026-05-21 — Phase D3: pure `network_diag.py` (DNS/TCP/`/health` checks + operator verdict strings) for master failure mode H; 17 pytest cases, no Tk.
- [[Implement-Phase-D1-Fix-Engine]] — 2026-05-21 — Phase D1: pure `fix_engine.py` Section 5 decision tree + `requires_admin_confirm`; `WorkerRuntimeState.to_health_snapshot()`; 24 pytest cases.
- [[Implement-Phase-D5-Master-GUI-Integration]] — 2026-05-21 — Phase D5: wires Fix/R/Rst/C tiles, SAP restart banner, mass WS-fail context, `FixActionDispatcher`, supervisor respawn + admin token injection into `master_gui.py`.
- [[Implement-Phase-H1-Connect-MVP]] — 2026-05-21 — Phase H.1 end-user **OmniFrame Connect**: frameless always-on-top CTk widget (`omni_agent/connect/`), single-worker watchdog on port 8765 (soft-fallback env), crash-loop circuit breaker, Job Object + psutil clean shutdown, `--version`/`--reset` CLI. H.3/H.4 deferred (position persist, packaging).
- [[Implement-Phase-H2-Self-Diagnostic-Friendly-Errors-Reset]] — 2026-05-21 — Phase H.2 Connect friendliness: warn-only launch self-diagnostic + 24h modal gate, friendly error modals + `sys.excepthook`, in-widget Reset + Open Logs, one-shot `/sap/sessions` system label on first green. H.3/H.4 deferred (widget polish, packaging/self-update).
- [[Implement-Phase-H3-Connect-Widget-Polish]] — 2026-05-21 — Phase H.3 Connect widget polish: drag-position persist (`connect_widget_pos.json`, atomic write), off-screen guard (pure clamp + optional screeninfo), hover tooltip, pill pulse via `widget.after`, paused amber cue, `resolve_subtitle_hint` priority resolver. H.4 deferred (packaging/self-update).
- [[Implement-Phase-H4-Connect-Self-Update-Packaging]] — 2026-05-21 — Phase H.4 Connect self-update + packaging: manifest fetch/parse, 24h poll + dismiss state, SHA-256 verified download, `--replace-helper` in-place EXE swap, third PyInstaller target `OmniFrame_Connect.exe`, `OmniFrame_Connect.zip`, operator runbook under `connect/build/`. 296 pytest passed (connect + master).

## Realtime / presence

- [[Harden-Presence-Service-Tenant-Overload]] — browser-side companion to the agent v1.8.4 fix. Six-fix surgical pass on `src/lib/presence/*` so a Supabase Realtime tenant overload (`Presence_shard112` GenServer crash on `c9d89a74`) can no longer be re-wedged from long-lived customer-portal tabs. `scheduleTrack()` debouncer + channel-error circuit breaker + visibility-aware DB heartbeat + `VITE_PRESENCE_DISABLED` kill switch + kiosk/RF route opt-out + sync-throttle bump. Same consumer hook surface; no schema changes. Phase B2 + B3 follow-up (same day) added permission-gated subscription via `presence:hidden` and dropped `current_page` from the broadcast payload (privacy + payload-size win); Phase B1 (multi-tab leader election) stays deferred. See [[Patterns/Realtime-Presence-Browser-Hardening]] for the distilled pattern (now six layers + privacy considerations), [[Debug/Fix-CustomerPortal-Presence-Tenant-Overload]] for the audit + diagnosis.
- [[Migrate-SapAgentChanged-To-Rust-WS]] — first Tier 1 follow-on from [[Roadmap-Rust-WS-Unlocks]] (2026-05-06). Retires the highest-fanout `supabase.channel(postgres_changes)` consumer in the app (`omniframe-agent-detection-fleet` + `sap-agents-fleet`). End-to-end: migration 270 emits `pg_notify('sap_agent_changed', …)` on every `sap_agents` row change → new `rust-work-service::sap_agents_listener` (mirror of `settings::listener`) → typed `WsEvent::SapAgentChanged { agent_id, organization_id, status, last_seen_at, op }` over the existing per-org WS fan-out. FE callsites drop their Supabase channels and paired 30s / 15s/60s polling timers; subscribe to the singleton `WorkServiceWebSocket` instead. Each consumer keeps a 5-min safety-net poll guarded on `getConnectionState() !== 'connected'`. Belt-and-braces FE-side org check on top of the Rust send loop's deny-by-default filter.
- [[Add-WsEvent-Lagged-Metric]] — ops ratchet shipped same day as the SapAgentChanged migration. New Prometheus counter `work_ws_lagged_events_total{org_hash}` on the per-socket `tokio::sync::broadcast` recv loop in `rust-work-service`. The `while let Ok(…)` was previously absorbing `RecvError::Lagged(n)` silently; the explicit `match` now records dropped events + emits `tracing::warn!` with the lagged count + org_id. Receiver auto-resyncs (we don't disconnect the slow consumer). Pairs with the migration above so we can SEE if event volume pushes the 1000-slot buffer.
- [[Implement-Presence-On-Rust-Option-2]] — **HEADLINE Option 2 from [[ADR-Presence-Architecture-Next-Steps]]** (2026-05-06). Replaces the org-wide Supabase Presence channel with a Redis-backed per-org HSET that `rust-work-service` owns end-to-end. New `presence` module (Redis schema, 30s tokio evictor), three new REST endpoints under `/api/v1/presence` (heartbeat, online, untrack), three new `WsEvent` variants (`PresenceJoined`, `PresenceUpdated`, `PresenceLeft`), three new Prometheus metrics. New FE class `PresenceServiceRust` with the same surface as `PresenceService`; module facade in `src/lib/presence/index.ts` selects via `VITE_PRESENCE_MODE` (`'supabase'` default / `'rust'` / `'disabled'`). Phase A / B2 / B3 surface intact. Distilled into the Pattern note [[Server-Side-Presence-Redis-HSET]] which Worker 3's Tier 2 entity-focus extension reuses.
- [[Migrate-Tier1-Deferred-Channels-To-Rust-WS]] — second Tier 1 wave (2026-05-06). Retires the four deferred-channel callsites the original [[Roadmap-Rust-WS-Unlocks]] said were "fine, defer until a complaint": `use-job-queue.ts`, `import-lt22-dialog.tsx`, `use-cycle-count-operations.ts`, `use-lx03-data.ts`. Adds four typed `WsEvent` variants (`SapJobStatusChanged`, `ImportRunStatusChanged`, `CycleCountOperationChanged`, `Lx03DataChanged`), four NOTIFY trigger migrations (271–274), four sibling `*_listener.rs` modules. Each FE consumer registers a single handler on the `WorkServiceWebSocket` singleton + a 5-min safety-net poll guarded on `getConnectionState() !== 'connected'`. Workstream B (`use-agent-trigger-runtime.ts`) grandfathered with an explicit exception in `realtime-policy.mdc`. Workstream C decided typed-vs-envelope ([[ADR-WsEvent-Typed-vs-Envelope]]) and Workstream D2 parked the `broadcast::channel` sizing decision ([[ADR-Broadcast-Channel-Sizing]]); Workstream D1 wrote the `ws-lagged-events.md` runbook so the previously-`(TODO)` pointer in the metric module is now live.
- [[Implement-Entity-Soft-Locking-Tier2-1]] — Tier 2 #1 from [[Roadmap-Rust-WS-Unlocks]] (2026-05-06). "Sarah is editing this row" pill on DataTables. Sibling subsystem to the presence work — separate Redis prefix (`presence:focus:*`), 30s TTL (half of presence's 90s), independent tokio evictor task, new `WsEvent::EntityFocus { entity_kind, entity_id, user_id, organization_id, action }` variant, three REST endpoints under `/api/v1/entity-focus/*`. New FE hook `useEntityFocus` + `EntityFocusPill` component. Wired on `TicketListPanel` as the canonical reference; pattern distilled in [[Patterns/Entity-Focus-Soft-Locking]] for incremental adoption on other DataTables.
- [[Implement-Notifications-Panel-Tier2-2]] — Tier 2 #2 (2026-05-06). Top-right bell-icon panel with 99+ badge + popover showing the latest 50 notifications (mark-read on click, mark-all-read button). Migration 275 extends `public.notifications` with `organization_id`, `kind`, indexes, an AFTER INSERT NOTIFY trigger, and tightened RLS. New Rust `notifications_listener` (PgListener) + REST routes (`GET /`, `POST /:id/read`, `POST /read-all`) + `WsEvent::Notification` variant. New FE `useNotifications` + `NotificationsPanel` (mounted in the authenticated layout). Backend Python helper `enqueue_notification(...)` in `api/services/notifications.py` with 60s in-process dedup. Component note: [[Components/NotificationsPanel]].
- [[Implement-Richer-Dispatch-Broadcast-Tier2-3]] — Tier 2 #3 (2026-05-06). Supervisor "broadcast to zone X / role Y / specific users" dispatch primitive. `WsEvent::PushedWork` extended in place with four optional fields (`target_zone`, `target_role`, `target_user_ids`, `broadcast_message`) — wire-compatible with existing single-user pushes. New Rust route `POST /api/v1/dispatch/broadcast` (server-side supervisor authz, org-scoped target resolution against `worker_heartbeats.current_zone` / `user_profiles.role`). New FE `BroadcastDialog` in the work-queue admin + extended `use-pushed-work.ts` consumer that branches on `isBroadcast` and shows a 12s toast for matching recipients. MVP UX flagged for product iteration.
- [[Implement-Rust-Work-Service-Phase0-Phase1]] — Phases 0 + 1 of the comprehensive [[plans/rust_work_service_full_integration_5b88165d.plan]] (2026-05-06). Pre-flight diagnostics (production health baseline, `/metrics` snapshot, agent-channel inventory, replica-identity audit) plus free-wins (FE bundle ships `VITE_PRESENCE_MODE=rust` ready to flip on Railway, three dead device-manager Realtime subscriptions deleted, `OMNIFRAME_AGENT_USE_RUST_WS=0` env-var stub added to the agent for Phase 4 wiring, `.env.example` documents the production rust-work-service URL).
- [[Implement-Rust-Work-Service-Phase2]] — Phase 2 (telemetry foundation) of the same plan (2026-05-06). Hard precondition for Phase 4. New Rust gauge `work_ws_broadcast_buffer_pct{org_hash}` sampled in the WS send loop on every `rx.recv()` (leading indicator that complements the after-the-fact `work_ws_lagged_events_total` counter). New Rust counter `work_http_requests_total{route,method,status}` populated by a new `track_http_metrics` axum middleware layered on the merged Router. Four Prometheus alerts (`WorkWsLaggedEventsSustained`, `WorkWsAuthFailureOrgMismatch`, `WorkServiceHealthFailing`, `WorkIdempotencyReplayDrift`). Three new runbooks (`auth-failure-org-mismatch.md`, `service-health-failing.md`, `idempotency-replay-drift.md`) plus an updated `ws-lagged-events.md`. 15-panel Grafana dashboard JSON. Draft availability SLO ADR ([[ADR-Rust-Work-Service-Availability-SLO]]) — 99.9% `/ws` over 30d, p95 < 200ms on `/api/v1/*`, zero-tolerance lagged-events + cross-tenant lines, multi-window burn-rate alerting; locked-in 2026-05-20.
- [[Implement-Rust-Work-Service-Phase7]] — Phase 7 (queue-claim path centralization) of the same comprehensive plan (2026-05-06). Routes the agent's hot-path queue writes (`claim` → `complete`/`fail` → `heartbeat`) through new `rust-work-service` endpoints under `/api/v1/sap-agents/jobs/...` instead of direct PostgREST. Same SQL functions wrapped (`claim_sap_agent_job`, `bump_sap_agent_job_lease`, `sap_agent_jobs` PATCH with v1.7.2 terminal-state guards) — the DB contract is unchanged; the difference is observability (4 new Prometheus metrics: `sap_jobs_claim_total{org_hash, outcome}`, `sap_jobs_claim_latency_ms{org_hash}`, `sap_jobs_complete_total{org_hash, outcome}`, `sap_jobs_fail_total{org_hash, step}`) and a future seat for Phase 10 (agent identity v2). Ships behind feature flag `OMNIFRAME_AGENT_CLAIM_VIA_RUST` (default `0`); per-claim parallel-run counters (`_claim_via_rust_total` vs `_claim_via_supabase_total`) print one `[claim-path] via=... totals(...)` line per claim for 24h grep parity. New capability `agent-claims-via-rust` advertised unconditionally. AGENT_VERSION stays at 1.9.0 — Phase 7 is invisible at default; the bump comes when Phase 11 flips the default.
- [[Implement-Rust-Work-Service-Phase4]] — Phase 4 (THE HEADLINE) of the same comprehensive plan (2026-05-06). Retires the OmniFrame on-prem agent's direct Supabase Realtime dependency and replaces it with the `rust-work-service /ws` bus. Net deletion target after parallel-run: ~400 LOC from `omni_agent/agent.py`. Migration 276 ships `notify_rf_putaway_changed` (idempotent NOTIFY trigger on `rf_putaway_operations` shipping `row_to_jsonb(NEW)` only); new `WsEvent::RfPutawayChanged` variant (REQUIRED `organization_id` covered by deny-by-default WS filter); new `rf_putaway_listener.rs` mirroring `sap_agents_listener.rs` 1:1 with defensive parse-error handling. New agent module `omni_agent/work_service_ws.py` (~280 LOC) — single asyncio thread, mints 5-min HMAC subscribe-token via `POST /api/v1/work/ws-token`, dispatches every event to caller's `on_event`. Reconnect ladder mirrors v1.8.4 Realtime semantics (5→60s additive, 60s+ stable resets) but **NO local circuit breaker** because the work service's own Phase 2 telemetry breaker is the authoritative reliability signal. `agent.py` wired with `_USE_RUST_WS` flag branching at the top of `_start_realtime_subscription()` — when True, calls `_start_work_service_ws_client()` and returns BEFORE spawning the legacy Supabase asyncio thread (the two paths NEVER both connect at once; parallel-run is across machines, not in-process). New `_on_work_ws_event(event_dict)` dispatcher routes `SapJobStatusChanged` → `_kick_job_poller(...)` and `RfPutawayChanged` → synthesized envelope + `_on_hardcoded_table_change(...)` (existing v1.6.4 evaluator stays unchanged). Parallel-run counters (`_work_ws_event_counts` vs `_legacy_realtime_event_counts`) bumped per delivery so the operator can compare rates over the 3-day window before flipping the default. New capability `rust-ws-client` advertised in `/health.capabilities`. `AGENT_VERSION = '1.9.0'` + matching `LATEST_AGENT_VERSION`. Deferred to v1.10: deletion of the legacy ~400 LOC (`_RealtimeCleanCloseTracker`, `_RealtimeCircuitBreaker`, `_realtime_cooldown_ladder`, `OMNIFRAME_DISABLE_REALTIME` env gate, `_supabase_request` retry layer); deferred to a separate Railway env change: `WORK_WS_REQUIRE_TOKEN=true` strict-mode flip after 1 week of stability.
- [[Implement-Rust-Work-Service-Phase3]] — Phase 3 of the same comprehensive plan (2026-05-06). Server-owned bootstrap-snapshot endpoints for the SAP-agent fleet + recent-job ledger so the FE no longer has to direct-query Supabase REST for either. New Rust routes `GET /api/v1/sap-agents/fleet` (server-scoped to org via `AuthenticatedUser.organization_id`, optional capability decode, hits `idx_sap_agents_online` partial index in 0.064 ms) and `GET /api/v1/sap-agents/jobs/recent` (compact `payload_summary` JSONB projection, `LEFT JOIN sap_agents` on `COALESCE(assigned_agent_id, claimed_by)` so unpinned-but-claimed jobs still surface a hostname label, 3.5 ms for 50 rows). New FE client `src/lib/work-service/sap-agents-client.ts`. `useAgentDetection.probeFleetOnce` + `agents-fleet-card.refresh` swapped to `getFleet(...)` with one-release Supabase REST fallback (TODO(Phase 11) to delete). New `RecentJobsCard` component mounted on the Agent Triggers tab below the Fleet & Diagnostics panel — bootstraps via `getRecentJobs({ limit: 50 })` + WS-driven refresh on `SapJobStatusChanged`. Schema deviation: `sap_agents` has no `user_id` column today, so the route returns `null` for `user_id` / `user_email` (wire shape preserved for a future migration).
- [[Implement-Rust-Work-Service-Phase5]] — Phase 5 of the same comprehensive plan (2026-05-06). Server-side defence-in-depth wrapper for the highest-risk SAP Testing surface: Material Master mutations. New Rust route `POST /api/v1/sap-mutations/material-master` lays five sequential checks in front of the `sap_agent_jobs` INSERT — (1) role gate (admin/superadmin/sap_mutator), (2) per-material concurrency lock `presence:lock:material:{org}:{material}` SET NX EX 300 → 409 on collision, (3) per-org rate limit `ratelimit:sap-mutations:{org}` INCR + EXPIRE 60s, > 10/min → 429 with `Retry-After`, (4) pre-flight `sap_audit_log` row at `status='pending'`, (5) `sap_agent_jobs` INSERT (carries `_audit_log_id` in payload + `idempotency_key` from header) + `UPDATE sap_audit_log SET job_id` link in same DB tx. Migration 277 extends `sap_audit_log_status_check` with the lifecycle states (`pending`/`completed`/`failed`/`canceled`) without dropping the legacy `success`/`error`/`warning` triplet so existing FE writers don't have to migrate in lock-step. `sap_jobs_listener.rs` adds `patch_audit_row_on_terminal()` side-effect inside the existing LISTEN loop — `WHERE job_id = $1 AND status='pending'` filter scopes it to Phase 5 audit rows specifically. New `ApiError::TooManyRequests { message, retry_after_secs }` variant with `Retry-After` header. New FE client `src/lib/work-service/sap-mutations-client.ts` + `useJobQueue.waitForJob(jobId, opts?)` so `inventory-management-tab.tsx` can route Material Master single + batch flows through the new endpoint and observe terminal status via the existing `WsEvent::SapJobStatusChanged` push (Phase 4 plumbing reused as-is). 18 new pure-logic unit tests in `sap_mutations` + 2 in `sap_jobs_listener`. Defends both Material Master endpoints today (`/sap/material-master-bin` + `/sap/material-master-storage-types`); other endpoints unchanged pending future scope expansion.
- [[Implement-Rust-Work-Service-Phase6]] — Phase 6 of the same comprehensive plan (2026-05-07). Fleet-wide live console streaming. Agent's `_console_relay_thread` (behind `OMNIFRAME_AGENT_CONSOLE_RELAY=1`) drains a 10 000-line `collections.deque` ring buffer in 50-line batches every ~500ms and POSTs to the new Rust route `POST /api/v1/sap-console/lines` (in `sap_console.rs` — separate file from `sap_agents.rs` to dodge parallel-Phase-8 collisions). Route fans each line out as `WsEvent::SapAgentConsoleLine { agent_id, organization_id, level, message, ts }` via the existing per-org WS bus; SAP Console card subscribes via the new sibling hook `useAgentConsoleStream` and renders lines with an optional agent-filter dropdown (sourced from `useAgentDetection().fleet.agents`). Per-agent rate limit `ratelimit:sap-console:{agent_id}` (100 req/min/agent INCR + EXPIRE) defends the broadcast channel from runaway prints; `persist=false` is the default (broadcast-only hot path), `persist=true` writes each line to the new `public.sap_agent_console_log` table (migration 278: `(organization_id, ts DESC)` + `(organization_id, agent_id, ts DESC)` indexes, RLS allows org members SELECT, no INSERT policy because writes go through the service-role JWT). Agent-side stdout/stderr proxy `_ConsoleRelayStream` mirrors EVERY print() into the buffer so 250+ existing callsites don't have to be rewritten — a single `_log()` callsite (boot banner) demonstrates the future-phase migration target. AGENT_VERSION stays at v1.9.0 — Phase 6 is additive behind a flag. Phase 10 will plug an `agent.console.write` permission gate into the route's `allow_console_write()`; Phase 11 will add the viewer endpoint `GET /api/v1/sap-console/lines?since=…` and pg_cron 7-day cleanup.
- [[Implement-Rust-Work-Service-Phase8]] — Phase 8 of the same comprehensive plan (2026-05-06). Consolidates the SAP Testing tabs' 4–5 React Query hooks (`useAgentDetection().fleet`, `useOnlineSapAgents`, `useJobQueue.watchedJobs`, ad-hoc `sap_audit_log` selects, ad-hoc `sap_agent_schedules` selects + the FE-side `agent_id → capabilities` map recomputation) into a SINGLE server-side joined endpoint. New Rust route `GET /api/v1/sap-testing/dashboard?include_audit=N&include_schedules=true` runs four parallel SQL queries via `tokio::try_join!` (online agents / in-flight jobs / recent audits / scheduled jobs) and derives the `fleet_capabilities` map from the agents result — zero extra round-trip. Total parallel SQL wall-clock ~0.16ms on the c9d89a74 tenant (slowest leg `recent_audits` at 0.159ms). New FE client `src/lib/work-service/sap-testing-client.ts` mirrors the Phase 3 / Phase 5 sibling-file pattern; new FE hook `src/features/admin/sap-testing/hooks/use-sap-testing-dashboard.ts` is a TanStack Query wrapper with `staleTime: 30s` + `refetchInterval: 5min` safety-net + WS invalidation on FOUR variants (`SapAgentChanged`, `SapJobStatusChanged`, `RfPutawayChanged`, `Notification`; `SapAgentConsoleLine` slot reserved for Phase 6). Narrow FE migration: "Online Agents" KPI tile in `agent-triggers-tab.tsx` + BatchModePanel "Pin to agent" picker in `inventory-management-tab.tsx` rerouted; legacy hooks STAY as Phase 11 fallbacks (`TODO(rust-work-service Phase 11)` grep target). 2 new unit tests on the parallel-aggregation contract + capability-map derivation. `feature-admin-sap` chunk: 423.41 → 424.82 KB (+1.4 KB). AGENT_VERSION stays at v1.9.0 per plan directive. Phase 9 (trigger evaluator rewrite) unblocks on this endpoint.


## Inbound / inventory operations

- [[Implement-Inbound-Part-Transfer]].
- [[Implement-Inventory-Management]].
- [[Found-Part-Transfer-Workflow]].
- [[Kit-BOM-Chains-Expedites-And-INCORA-Component]].
- [[Authorized-Ship-Short-Negates-Black-Hat]] — 2026-05-12 — `Authorized to Ship Short` part numbers on the Add Kit Build Plan dialog now negate the auto-Black-Hat for the matching BOM line, so a kit explicitly authorised to ship without a part is no longer blocked from RF picking. Updates `bomCoverage` memo + `recheckBomCoverage` symmetrically; UI gains an amber `Ship Short` badge per BOM row that's covered only by the authorisation list.
- [[Edit-Ship-Short-Post-Creation-Flow]] — 2026-05-12 — Adds an `Edit Ship Short` button on the Kit Build Audit Trail dialog (row-click on Kitting Data Manager) so operators can mutate `authorized_ship_short_items` after creation. Save calls the new `RRKittingDataService.updateAuthorizedShipShortItems`, which writes the column on every TO row of the kit and re-runs `recheckBomCoverage` — auto-clearing the Black Hat when the saved list now covers every missing BOM line.
- [[Black-Hat-Ship-Short-Authorization-Panel]] — 2026-05-18 — Inline authorization panel inside the Kit Build Audit Trail (Quick View) shown only when a kit currently has an active Black Hat flag. Renders the structured list of unmatched BOM components (from new pure-read `RRKittingDataService.getMissingBomComponentsBySerial`) with a per-row Authorize checkbox + justification input; saves merge into existing `authorized_ship_short_items` and re-run BOM coverage so the Black Hat auto-clears. Migration 312 extends `kitting_workflow_settings` (sibling pattern to [[Optional-Kit-Inspection-Toggle]]) with three boolean policy flags (`black_hat_ship_short_authorization_enabled`, `…require_justification`, `…require_line_by_line_approval`, all DEFAULT TRUE), surfaced as a new sub-section in the existing Workflow Settings card. INCORA Sub-Kit rows are correctly rendered disabled (matcher excludes sub-kits from ship-short coverage). Legacy top-bar `Edit Ship Short` button stays as the free-form power-user shortcut — panel is additive.
- [[Persist-Kit-Notes-Chat-Thread]] — 2026-05-18 — The Kit Notes chat panel inside the Kit Build Audit Trail was decorative client-state — typed messages vanished when the dialog closed, no kit/user tracking. Migration 313 introduces `public.kit_notes` (org-scoped RLS, immutable append-only, sender snapshot, `event_kind` categorization). New `KitNotesService` + `useKitNotes` hook (TanStack Query, 10s `refetchInterval` for cross-user freshness — no new Supabase Realtime channel per the project's Realtime Policy). Dialog now persists user messages and stamps every in-dialog event (flag add/remove/clear, ship-short authorize from the legacy button + the new Black Hat panel) as a system note. Synthetic "Kit build plan created by X" entry is derived from `kit_added_*` columns at render time and self-dedupes if a real `kit_created` note ever lands. Bubble colour now keys on `isMine` (current-user comparison) so the right-aligned blue bubble follows the user across sessions/devices. System messages render italic. `formatChatTime` upgraded to 'Just now / Xm / Xh / Xd / dated' granularity.
- [[Non-Warehouse-Bin-Acknowledgment]] — 2026-05-18 — TO rows imported into a kit build plan sometimes carry a `sourceStorageBin` like `112NEEDBIN` that lives at the plant rather than inside our warehouse. RF Picking can't satisfy those rows so the operator owns running them down on the plant side, but there was no UI surfacing it. Migration 314 adds `non_warehouse_bin_patterns TEXT[] DEFAULT '{NEEDBIN}'` to `kitting_workflow_settings`. New pure helper `detectNonWarehouseBins` (10/10 unit tests pass) drives a new `NonWarehouseBinNotice` inline card inside the Add Kit Build Plan dialog (submit gated on the operator's acknowledgement) and a new `NonWarehouseBinConfirmDialog` for the Append TOs to Kit flow. Workflow Settings card on the Kitting Apps Settings tab gains a "Non-Warehouse Bin Patterns" sub-section with add/remove badges (uppercased / deduped / normalised before persist via `normaliseBinPatterns`). Matches are substring, case-insensitive — `NEEDBIN` triggers on `112NEEDBIN`, `R0NEEDBIN`, anything containing the marker. Default org behaviour is unchanged unless a matching row is imported.
- [[Cancel-Kit-TO-Line]] — 2026-05-21 — Per-line cancellation on `RR_Kitting_DATA` (migration 325 adds `cancelled` / `cancelled_at` / `cancelled_by_user` / `cancelled_reason` with a CHECK constraint enforcing all-or-nothing population + non-empty reason, plus a partial index on `(kit_serial_number) WHERE cancelled = FALSE` for the predominant active-line reads). New `RRKittingDataService.cancelTOLine(toLineId, reason)` static method. Both `getKitBuildPlanDetailsBy*` methods + `recheckBomCoverageBySerial` + `KitKanbanService.computeKitProgress` now filter cancelled lines from `totalLines` / `pickedCount` / `kittedCount` / `toMaterials` so a cancelled TO doesn't block the kit from advancing through Picking → Kitting → On Dock and doesn't fictitiously cover BOM components. New `CancelTOLineDialog` (modal capturing the operator's reason in a `<Textarea>`, max 500 chars, Confirm disabled until non-empty trim) integrated into the Kit Build Audit Trail's TO Lines table as a per-row icon-only Trash2 ghost button. Cancelled rows render with a muted background, line-through cells, em-dashed Picked/Kitted columns, and a destructive-tint `Cancelled` pill on the Material cell with `title` carrying `reason · actor · timestamp` for hover detail. Cancellation also stamps a system note on the kit_notes audit trail (`event_kind = 'to_line_cancelled'`) via the existing [[Persist-Kit-Notes-Chat-Thread]] hook. Migration applied to `wncpqxwmbxjgxvrpcake`; `tsc` + `eslint` + `prettier` clean.
- [[Configurable-Deliver-To-Plant-Locations]] — 2026-05-20 — Lifts the previously hardcoded `PLANT_LOCATIONS` 8-entry constant out of `src/components/ui/add-kit-build-plan-dialog.tsx` onto `kitting_workflow_settings.deliver_to_plant_locations TEXT[]` (migration 324; default seeds the same 8 values so existing org behaviour is verbatim). New `useDeliverToPlantLocations()` convenience hook wraps the existing `useKittingWorkflowSettings` shared query (no new Supabase Realtime channel — sibling pattern to [[Non-Warehouse-Bin-Acknowledgment]]). New pure helper `src/lib/kitting/plant-locations.ts` exposes `DEFAULT_PLANT_LOCATIONS`, `normalizePlantLocations` (trim, drop blanks, case-insensitive dedupe with first-seen casing preserved, **does NOT uppercase** because plant labels are user-facing), and `withCurrentPlantOption` (keeps a saved-but-removed value visible in the dropdown so editing an old kit doesn't render an empty Select). Settings UI gains a new sky-trim **Deliver To Plant Locations** sub-section in the Workflow Settings card, directly under the existing amber Non-Warehouse Bin Patterns section. Dialog Select disables itself + shows `No plants configured — add some in Settings` when the list is empty. **11/11** new vitest cases on the pure helper plus the existing 10 non-warehouse-bin tests still pass. Migration applied to `wncpqxwmbxjgxvrpcake` via Supabase MCP; column verified `ARRAY NOT NULL` with the 8-entry default.
- [[Redesign-Kit-Build-Audit-Trail-Layout-2026-05-20]] — 2026-05-20 — UI-only refactor of `KitProductionTrackerDialog` (`src/components/kitting/kit-production-tracker.tsx`). Dialog widened to `max-w-[1600px] min-w-[1280px]` with a sticky bordered header. Status badge promoted inline with title; refresh demoted to a ghost icon-only. Kit Information + Timeline & Dates merged into a single **Kit Overview** card (5-col grid, small-caps labels, mono identifiers, Priority pill on the header strip). Body switched from a 2-col layout (Progress | Flags+Notes stacked) to a 3-col `grid-cols-12` layout: Progress (col-span-4) | Flags (col-span-3) | Notes (col-span-5). Kit Notes chat panel made taller (`h-[520px]`, was 320px). Card chrome unified across all sections (`border-b pb-3` header divider, `text-base` titles, `h-4 w-4 text-muted-foreground` icons). Zero functional changes — every handler, hook, motion variant, and child-dialog mount preserved verbatim. `tsc` + `eslint` + `prettier` clean.
- [[Add-TO-To-Clear-Black-Hat]] — 2026-05-19 — Sibling on-ramp to [[Black-Hat-Ship-Short-Authorization-Panel]]: new "Add TO to Clear Black Hat" button in the panel header opens an `AddTOForBlackHatDialog` where operators paste SAP TO rows. Per-row preview labels each parsed TO as `Covers missing` (green) or `Extra` based on case-insensitive material-number match against the kit's current unmatched BOM components; non-warehouse bin acknowledgement is reused verbatim from the existing notice. Submit routes through `RRKittingDataService.appendTOsToKit` which dedupes by transfer-order-number per-serial, syncs the kanban totals, and runs `recheckBomCoverageBySerial` — Black Hat narrows (note re-stamped) or clears outright depending on whether every missing component is now covered. The two on-ramps (per-line Ship-Short authorize + Add TO) are additive: a kit with two missing materials can be cleared by mixing methods, since the matcher ORs together TOs, INCORA items, and ship-short authorizations. Parent dialog's `handleBlackHatPanelSaved` now branches on a discriminated `event` so the audit-trail chat thread stamps the right system note (event kinds: `black_hat_panel_authorized`, `black_hat_panel_to_added`).
- [[RF-Kit-Floor-Pick-Visual-Confirm]] — 2026-05-17 — `Confirm Location` step in `rf-kitting-picking-form.tsx` now branches by bin prefix: floor bins (K/S, no printed barcode) get a visual-confirm UI — giant `text-5xl/6xl` bin label + a new `HoldToConfirmButton` that requires a ~800ms press-and-hold to advance. Rack bins (R) keep the scan-to-confirm flow verbatim. Internal `currentStep === 'scan_location'` preserved on both branches (no telemetry regression). New pattern note [[Hold-To-Confirm-Gesture]] documents the gesture recipe (RAF-driven fill, pointer cancel, keyboard parity, latch on completion). Service write paths untouched; no new Realtime channels; no schema changes. `feature-rf-interface` chunk +4.46 KB (516.42 → 520.88).
- [[RF-Kit-Pick-By-Serial-Number]] — 2026-05-17 — RF Kit Picking entry step now smart-detects `kit_serial_number` (`KIT-YYYYMMDD-NNN`) and bypasses the `kit_select` disambiguation screen for multi-kit POs. New `RFKittingPickingService.verifyKitForPickingBySerialNumber` (direct PK lookup, no `kits[]` payload) + `isPotentialKitSerialNumber` helper; existing `verifyKitForPicking(kitPoNumber, kitSerialNumber?)` signature preserved verbatim so the legacy PO path + multi-kit picker still work for PO-labelled prints already on the floor. Scan-step copy / placeholder / helper text updated to `Scan Kit Serial Number` (`KIT-…` is the primary cue; PO accepted as fallback). The auto-detect inside the separate delivery-scan `rf-picking-form.tsx` now also probes the serial path first so a kit-serial scan into the delivery field hands off to Kit Picking. Kit Build Sheet QR now encodes the `kit_serial_number` (PO stays visible in the big header text + new small mono caption for the serial). +3 unit tests; pre-existing 5 still pass; one pre-existing date-bomb test failure in the same file is unrelated.
- [[RF-Build-Kit-By-Serial-Number]] — 2026-05-17 — RF Build Kit entry step now smart-detects `kit_serial_number` and routes through a new direct PK lookup `RRKittingDataService.verifyKitForBuildBySerialNumber`; legacy `verifyKitForBuild(kitPoNumber)` signature preserved verbatim. Shared private `assembleBuildKitPayload` helper extracted so the PO-path and serial-path behave identically once rows are loaded (status sanity check, user-name decoration, TO-line mapping). `useBuildKitTool` hook gains `verifyKitBySerialAsync` / `isVerifyingKitBySerial` exports mirroring the existing triplet. Form copy mirrors the picking sibling: heading `Scan Kit Serial Number`, label `Kit Serial Number or PO`, placeholder `Scan KIT-YYYYMMDD-NNN or Kit PO`. Post-load state machine + step keys untouched (telemetry coupling); downstream `startKitBuild` / `kitMaterial` / `completeKitBuild` still PO-keyed and operate on the `kitData.kitPoNumber` surfaced by either path. +3 unit tests on the new service method; pre-existing 8 still pass; same date-bomb failure as the picking sibling remains pre-existing. `feature-rf-interface` chunk: 520.88 → 522.64 KB (+1.76 KB).
- [[Optional-Kit-Inspection-Toggle]] — 2026-05-17 evening — Adds an org-scoped **Require Kit Inspection** toggle to the Kitting Apps Settings → Dropdowns sub-tab (Workflow Settings card at the bottom). When OFF, `completeKitBuild` jumps `in_progress → kit_inspected + on-dock` in a single UPDATE (stamps inspection + on-dock columns together so the audit trail stays coherent if the flag is later flipped back on); the Quality Check kanban column is hidden, the RF `Inspect Kit` tile is removed from the kitting-apps menu, and the `KitProductionTracker` timeline omits the Inspection step. Persistence is a new `kitting_workflow_settings` table (org_id PK, boolean columns; cleaner long-term home for siblings like `require_build_sheet_print`). Default `true` → existing org behaviour preserved. New `useKittingWorkflowSettings` hook, 5min `staleTime`, no new Realtime channels. Service signature kept additive (`completeKitBuild(kitPo, serial, options?)`); the multi-kit-per-PO serial scoping from [[Fix-Build-Kit-Completion-Multi-Kit-PO]] is preserved verbatim. `feature-rf-interface` chunk +2.24 KB (523.83 → 526.07). +2 unit tests on the bypass UPDATE shape.
- [[Kit-Kanban-Inspection-Aware-Progress-And-Dock-Completion]] — 2026-05-17 night — Two-part UX-correctness fix layered on top of [[Optional-Kit-Inspection-Toggle]] + [[RF-Dock-Staging-Flow]]. (1) The per-card mini progress bar on `kit-kanban-board.tsx` now filters out the **Insp** step when the org runs `kit_inspection_required = false` (matches the `KitProductionTracker` dialog's existing behaviour); `'inspection'` is remapped to `'kitting'` for inspection-disabled orgs so the active-step indicator stays meaningful while the kit awaits dock staging. (2) `RRKittingDataService.stageKitToDock` now also writes `kit_build_status: 'completed'` on its serial-scoped UPDATE and immediately calls `KitKanbanService.syncKitProgressFromSerial`; that sync helper now resolves the `column_name = 'completed'` column id and flips the kanban task's `column_id` + `position_in_column` whenever the per-serial computed `currentStep === 'on_dock'`. Canonical lane invariant: **on dock = done**, regardless of org settings. Backward-compat: kits stamped on-dock by the OLD skip-inspection branch (with `kit_dock_location = NULL`) still land in Completed on next board mount via `syncAllInProgressTasks`. New `syncKitProgressFromSerial` regression test + `stageKitToDock` test extended for the new `kit_build_status` write. Touched files: `kit-kanban-board.tsx`, `kit-kanban.service.ts`, `rr-kitting-data.service.ts`, `kit-serial-scoping.test.ts`. `kit-kanban-board` chunk +0.30 KB; `feature-rf-interface` 533.97 → 547.48 KB (rr-kitting-data is shared with the RF chunk; pre-existing oversize bucket per the bundle gate, out-of-scope per the brief).
- [[Print-Cover-Sheet-From-Audit-Trail]] — 2026-06-02 — New **Print Cover Sheet** button in the `KitProductionTrackerDialog` (Kit Build Audit Trail) sticky footer, beside **Delete Kit**, for a quick reprint of the printable `KitBuildSheet` without leaving the trail. Enhanced `KitBuildSheet` with an optional `kitSerialNumber` prop: when present it loads via `getKitBuildPlanDetailsBySerialNumber(serial)` instead of the PO-resolving `getKitBuildPlanDetails(po)` so multi-kit POs reprint the exact kit the trail is scoped to (per [[Kit-Serial-Scoping]]); the load-once ref + open-effect key on `kitSerialNumber ?? kitPoNumber`. Backward-compatible — the kanban Start-Kit flow still passes PO-only. Nested Radix dialog (same pattern as the sibling ConfirmDialog / EditShipShortDialog). `tsc -b` + ESLint clean; FE-only.
- [[Kit-Number-On-Kanban-Card]] — 2026-06-01 — Kit Assembly Board cards now render the human-readable **kit number** (`Kit {kit_number}`, e.g. `Kit 424 Inlet #1`) under the title. `kit_number` is not a column on `kit_kanban_tasks` (verified via Supabase MCP) — it lives on `RR_Kitting_DATA` — so `KitKanbanService.getTasksByColumn` batch-enriches it keyed by `kit_serial_number` per [[Kit-Serial-Scoping]] (no PO fallback), plus a new `getKitNumberBySerial` single-serial lookup for realtime INSERTs. Board preserves the enriched `kitNumber` across realtime UPDATEs (the payload omits it) and patches freshly-inserted cards async. `kitNumber` was already on the `KitKanbanTask`/`KitTask` types so no plumbing needed; memo comparator extended. New `Boxes` icon. FE + service-only, no migration. `tsc -b` + ESLint clean; the lone `kit-serial-scoping.test.ts` failure is the unrelated pre-existing date-bomb (`generateKitSerialNumber` daily reset).
- [[RF-Dock-Staging-Flow]] — 2026-05-17 night — New 4th tile on the Kitting Apps RF menu (`Truck` icon, green border) drives a 5-step RF flow: scan kit serial/PO → optional disambiguation → kit summary → scan dock barcode (or tap to select for damaged barcodes) → confirm. Persistence: (a) reuses `kitting_dropdown_options` with a new `option_group = 'dock_location'` (CHECK constraint extended; `seed_kitting_dropdown_options` seeds `DOCK-1` / `DOCK-2`); (b) new nullable `RR_Kitting_DATA.kit_dock_location TEXT`. New service methods `verifyKitForDockStaging` + `stageKitToDock` — both serial-scoped (preserves [[Fix-Build-Kit-Completion-Multi-Kit-PO]]). Dock-ready predicate splits on `kit_inspection_required`: ON → require `kit_inspection_completion_date_time`; OFF → require build complete. **Correction to [[Optional-Kit-Inspection-Toggle]]** — the skip-inspection branch in `completeKitBuild` previously co-stamped `kit_ready_on_dock_*` alongside the inspection columns; that has been removed so on-dock is now ALWAYS captured by this new RF flow regardless of inspection mode. Existing skipInspection tests flipped (assert on-dock columns are `undefined` on the bypass UPDATE); +8 new dock-staging tests. New `useDockStaging` hook; no Realtime channels. `feature-rf-interface` chunk: 526.07 → 533.97 KB (+7.90 KB).
- [[Fix-Build-Kit-Completion-Multi-Kit-PO]] — 2026-05-17 evening — Closed the open follow-up on [[RF-Build-Kit-By-Serial-Number]]. Operator on RF Build Kit kitted all 31 lines of `KIT-20260515-002` on PO `2010102616`, hit Complete, got `Cannot complete kit: 18 lines still need to be kitted` — `completeKitBuild`'s aggregate check + final UPDATE were scoped to `kit_po_number` alone and saw the sibling `KIT-20260515-001`'s 18 unstarted rows. Same class of multi-kit-per-PO cross-link bug as [[Fix-Kit-Build-Cross-Linked-Parts]] but on the Build Kit service paths. Additive fix: `completeKitBuild` / `startKitBuild` / `kitMaterial` gained an optional `kitSerialNumber` parameter that scopes every internal query by serial when supplied (PO-only behaviour preserved when absent for backward compat). `assembleBuildKitPayload` now surfaces `kitSerialNumber` on `kitData`; both Build Kit forms (RF + legacy desktop) pass it through. `markLineAsKitted` / `unmarkLineAsKitted` remain line-id-keyed but route kanban sync through `syncKitProgressFromSerial`. +3 new regression tests on `completeKitBuild` multi-kit-per-PO scoping; date-bomb test still the one unrelated failure. `feature-rf-interface` chunk: 522.64 → 523.83 KB (+1.19 KB).
- [[Kit-Build-Plan-Completed-Tab]] — 2026-06-04 — Splits the **Kit Build Plans** grid (`KittingDataManager`) into shadcn `<Tabs>`: **Open Work** (non-`completed`, drag-to-reorder preserved) and **Completed** (`kit_build_status = 'completed'`, read-only) with count badges, so completed/on-dock kits no longer clutter the open-work prioritization queue. `KittingDataGrid` gained `reorderable?` (default true → new `StaticRow` + no `DndContext` when false) and `emptyMessage?` props. Merge-safe `handlePriorityChange` fix: the open-work subset is now spliced back with the untouched completed rows on optimistic reorder (the old `setGridData(reorderedRows)` would have dropped completed kits until next fetch). FE-only, no migration; `tsc -b` + ESLint clean.
- [[Scannable-TO-Barcodes-On-Build-Sheet]] — 2026-06-04 — Each TO number in the printable `KitBuildSheet` "TO's" table now renders a scannable **Code 128** barcode beneath it. New dep `jsbarcode` (`3.12.3`), **dynamically imported** so it code-splits into its own lazy ~68 KB chunk (loads only when the sheet opens). Barcodes are drawn into a detached `<canvas>` → PNG data URL → `<img>` (survives the `handlePrint` innerHTML copy, mirroring the existing kit QR), keyed in a `toBarcodes` state map; inline `<img>` styles for print fidelity (the print window ships only a hardcoded utility-class subset). `tsc -b` clean; the bundle-budget gate's failures are pre-existing and unchanged. FE-only, no migration. **2026-06-11 fix:** the original `width: 1.4` barcodes did not actually decode — see [[Fix-Build-Sheet-TO-Barcode-Not-Scannable-Fractional-Width]] (integer width 2, 2× height, baked-in quiet zones; verified with zxing-cpp).
- [[Kit-Build-Plans-Grid-Reorder-ShipShort-Unread]] — 2026-06-04 — Kit Build Plans grid (`KittingDataGrid`): (1) column reorder → Priority · Date Added · Due Date · Kit Serial # · Kit PO · Kit Number · Status · Ship Short · Messages · Flags (dropped "Added By", kept Flags); (2) new **Ship Short** column (amber part-number badges from `authorized_ship_short_items`, threaded through `getKitGridData`); (3) per-user **unread-message indicator** — migration **330** adds `kit_note_reads` (per-user/per-kit read watermark, own-row RLS) + SECURITY INVOKER RPC `kit_notes_unread_serials()` (operator notes from others, newer than the caller's watermark). Grid `New` badge via TanStack `meta`; `KitProductionTrackerDialog` marks read on open (covers grid + kanban); polled 30s, no realtime channel. Applied to `wncpqxwmbxjgxvrpcake`; advisors clean; `tsc -b` + ESLint clean.
- [[Build-Sheet-Authorized-Ship-Short-Section]] — 2026-06-04 — Printable `KitBuildSheet` now shows an **Authorized Ship Short** section under Transport Initials (Part Number + **Authorized By**), only when the kit has ship shorts. Key finding: `authorized_ship_short_items.authorizedBy` (migration-101 JSONB format) was never populated — so now stamped at write time: `createKitBuildPlan` (creator) + `updateAuthorizedShipShortItems` (merge-preserve prior authorizer, stamp current operator for newly-added parts) via new `getCurrentUserDisplayName()`; read types widened to carry it. Print fidelity via inline border/bg styles (print window lacks `border-r`/`bg-black`). Legacy items render `—` until re-saved. FE+service only, no schema change; `tsc -b` + ESLint clean.
- [[Build-Sheet-Print-Fit-Letter-Landscape]] — 2026-06-05 — `KitBuildSheet.handlePrint` now prints to a single **8.5x11 landscape** page: `@page size: 11in 8.5in` (was A4 landscape) + a measure-and-scale `fitToPage()` that lays the content out at the printable width, scales by `min(availW/w, availH/h, 1)` (never upscales), and collapses `#print-scale-wrapper` to the scaled footprint so the CSS transform doesn't leave a full-size box that paginates. Guarded single `printOnce()`. Robust to data-driven height growth (TO barcodes, ship-short rows). Print-window only; `tsc -b` + ESLint clean.
- [[Kit-Build-Plans-Expedites-Tab]] — 2026-06-06 — Kit Build Plans grid tabs renamed to **Open Work Kits** / **Completed Kits** + new **Expedites** tab for stand-alone single-part expedites (`engine_program = 'EXPEDITE'`, mode 2 of `addExpediteToKit`). Threaded `engine_program` through `getKitGridData` → `KittingGridRow`; manager splits into expedite/open/completed memos (expedites excluded from both kit tabs, any status; read-only grid). Fixed `handlePriorityChange` to merge the reordered open-work subset back **by id** (was keeping only completed rows — would have dropped expedites from local state). FE+service only, no schema change; `tsc -b` + ESLint clean.
- [[Add-Expedite-Part-TO-Import]] — 2026-06-06 — Reworked the expedite dialog (renamed **Add Expedites to Kit Build Plan** → **Add Expedite Part**) to use the same clipboard TO import as the normal Add to Kit Build Plan; dropped manual part/qty/description — each imported TO row becomes one stand-alone expedite part in the Expedites tab (`material`→part number, qty, `transfer_order_number` stored). Only delivery time (+ reason/date) is entered, applied to all parts. New `addExpeditePartsFromTOs` loops `addExpediteToKit` (now accepts `transferOrderNumber`); removed the attach-to-Kit-PO option + `expediteKitPoOptions`. FE+service only, no schema change; `tsc -b` + ESLint clean.
- [[Kit-Grid-Derived-Stage-Status]] — 2026-06-07 — Kit Build Plans grid Status column now shows the real current stage instead of the coarse `kit_build_status` (which sat on "In Progress" through picking + kitting). `getKitGridData` aggregates per-kit picked/kitted/inspected/on-dock progress while grouping by serial and derives a granular stage (`picking`/`picking_complete`/`kitting`/`kit_built`/`kit_inspected`/`completed`, else raw) into a new display-only `kit_stage_status`. Raw status untouched (tab/search logic preserved). `StatusBadge` gained picking/picking_complete/kitting; search + CSV use the derived stage. FE+service only, no schema change; `tsc -b` + ESLint clean.
- [[Kitting-Completed-Stat-Card-Date-Buckets]] — 2026-06-11 — Kitting Data Manager's **Completed** stat card now shows EST date-scoped buckets — **Today / Yesterday / This Week (rolling 7 days)** — with the all-time total moved into the card header; restyled to the outbound-data-manager pattern (`KpiGrid` + `StatTile`, icon chip, hover gradient). `getStatistics()` derives DISTINCT-kit buckets from `kit_ready_on_dock_date_time` (completion moment per the on-dock = done invariant) converted to EST calendar dates via `@/lib/utils/timezone`; legacy `'completed'`-status kits without a dock timestamp count all-time only. FE+service only, no schema change; lints clean.

## Warehouse map

- [[DXF-Import-Warehouse-Map]].
- [[Warehouse-Map-Phase-A-D-Complete]].
- [[Warehouse-Map-Edit-Toolbar-And-Polish]].
- [[Warehouse-Map-3D-View-Mappedin-Style]].
- [[WarehouseMapWidget-Embeddable-Mini-Map]].

## Productivity / shifts / standard work

- [[Redesign-ShiftProductivity-Associate-Row]] / [[Redesign-ShiftProductivity-Settings-Operations-Control-Center]].
- [[Redesign-StandardWork-Comprehensive]].
- [[Implement-Production-Boards-Hourly-Grid]] — 2026-05-10 — new sidebar entry **Production Boards** under Labor Management with a TV-display-grade Hourly Completion Tracker (per-associate × per-hour grid). `?tv=1` mounts a fullscreen overlay with screen Wake Lock + idle-cursor-hide; data via two new public wrappers on `TeamPerformanceService` (`getActivityEventsForDate`, `getShiftAssignmentsRaw`) bucketed client-side by `bucketEventsByHour`. Polling-only (visibility-gated 30s) per [[realtime-policy]]; reuses the `shift_productivity:view` permission (no new key). Migration 292 inserts the nav row + role visibility. See [[ProductionBoards - Feature Module]].
- [[Implement-SQCDP-TV-Viewport-Fit]] — 2026-05-17 — SQCDP TV-mode layout pass: every primary card was floating its value mid-card from a `min-h-[16rem]` worst-case reserve, and the grid wrapper did not fill `<TvFrame>`'s viewport. Replaced the static `space-y-*` wrapper with a `flex h-full flex-col` chain, gave the primary / secondary rows `flex-5` / `flex-3` weights + `auto-rows-fr`, and swapped the per-card `min-h-[16rem]` reserve for a `flex-1 items-end` chain so the value bottom-anchors against the row's stretched height instead of a hardcoded floor. Distilled into the [[TV-Viewport-Fit-Grid]] pattern.
- [[Implement-SQCDP-Hero-Autofit-Typography]] — 2026-05-17 — **SUPERSEDED same day by [[Implement-SQCDP-Measured-Hero-Typography]]** (v15.2, via v15.1).  Originally superseded by v15.1 (vh-based) the same day, which itself was superseded by v15.2 (measured-fit hook) the same day. Initial v15 follow-on after v1 viewport-fit. Added TV-only auto-fit hero typography via CSS container queries (`cqh`) — pure CSS, no JS. After a 1080p TV review the user reported visibly inconsistent glyph sizes across siblings (each card scaled to its own value-block height, which differs by content/wrap/trend-arrow presence). v15.1 swaps `cqh` for `vh` so every card resolves to the same font-size on a given viewport. Kept here for historical record; new work should follow v15.1.
- [[Implement-SQCDP-Uniform-Hero-Typography]] — 2026-05-17 — **SUPERSEDED same day by [[Implement-SQCDP-Measured-Hero-Typography]]** (v15.2). Same-day correction to the v15 cqh-based recipe. User asked for "pure consistency across the board, where everything resizes in the correct ratio as the screen gets larger" after a 1080p TV review showed visibly different glyph sizes across the five primary cards despite identical class chains. Replaced `cqh` (container-query height — resolves against each card's own value-block height, drives per-card variation) with `vh` (viewport-relative — every card sees the same input, siblings render uniformly while still scaling smoothly across 1080p / 1440p / 4K). New constants `TV_UNIFORM_*` (renamed from `TV_AUTOFIT_*` to reflect the actual mental model): `text-[clamp(4.5rem,10.5vh,12rem)] leading-[0.95]` for primary, `text-[clamp(2.25rem,5.5vh,5.5rem)] leading-[1]` for sub-metrics, `h-[7vh] w-[7vh] min-h-[2rem] max-h-[4.5rem]` for the trend arrow. Dropped the `containerType: 'size'` inline style on both single-mode wrapper and each `<SubMetricBlock>` (no per-card containment boundary needed). Tightened the 4K ceiling vs v15 (16rem → 12rem) because uniform sizing means EVERY card reaches the max. Curator size overrides still win. Editor hint copy updated. 156 SQCDP unit tests green (was 155; +1 new "uniform across the row" test that mounts `<SqcdpGrid>` with all 5 primary builtins and asserts every hero element receives the SAME size-token class string). Decision log [[ADR-SQCDP-Uniform-vs-AutoFit-Typography]]. Pattern [[TV-Viewport-Fit-Grid]] extended with a v15.1 subsection that supersedes the v15 cqh recipe.
- [[Implement-SQCDP-Editor-Fine-Grained-Controls]] — 2026-05-17 — v14 SQCDP editor pass. Adds a dirty-aware tab strip (per-tab unsaved-changes dot badges + one-line tab description), a footer "Unsaved changes" pill, dialog width bump to `1180px`, and a stacked-vs-single preview toggle in the live-preview aside. Schema extensions: per-field `align` / `letterSpacing` / `color` (free-form hex via inline style — the carveout to the [[Per-Field-Style-Overrides]] JIT-safe class rule), `transform: 'lowercase'`, and a new top-level `header` sub-config (`height: compact/normal/tall`, `align: left/center`, `showIcon: boolean`) controlling the colored category band. `ChartConfig.grid.opacity` slider (0–50%) feeds `<CartesianGrid strokeOpacity>`. Style tab gains a recommended-palette strip + WCAG contrast badge; Chart tab gains ToggleGroups for chart type / curve / line-style + sliders for grid opacity / target line width + "Auto" chips on Y-axis min/max. New shadcn primitives `<Toggle>` + `<ToggleGroup>` installed via the shadcn MCP. All 113 SQCDP unit tests green (+12 new); zero new lint warnings.
- [[Fix-Shift-Productivity-Cycle-Count-Filter]] — 2026-05-12 — forward
  migration `304_fix_team_activity_events_cycle_count_filter.sql`
  resolves Bug A (events RPC filtered cycle counts by `created_at`
  while counts RPC filtered by `completed_at` — dropped 95% of today's
  completed counts from the Gantt because counts are mass-created in
  nightly/weekly batches) and Bug C (status allowlist widened to
  include `variance_review`). Re-creates both
  `get_team_activity_events` and `get_team_productivity_counts` in
  full so they stay byte-equivalent to migration 188 outside the
  cycle-count arm. David Simmons events arm went 0 → 34 cycle counts;
  org-wide drop ratio 164/173 (active users) → 0/173. Bug B (phantom
  assignees / Marvin Berry-style) remains open pending product
  decision. See [[Investigate-Shift-Productivity-Cycle-Counts-Hidden]].
- [[Implement-SQCDP-Editable-Categories]] — 2026-05-17 — retires the global `sqcdp_category` Postgres ENUM and replaces it with a per-org `production_board_sqcdp_categories` table. Curators (gated on `production_boards:edit`) get a `<SqcdpCategoryManagerDialog>` for add / hide / delete / reorder / reset-to-defaults, plus a search-as-you-type combobox in the metric editor with sticky-footer "+ New" / "Manage…" entry points. Migration 306: new table + RLS + per-org seed of the 9 builtins + `AFTER INSERT ON organizations` trigger so future tenants are auto-seeded + composite FKs `(organization_id, category) → production_board_sqcdp_categories(organization_id, slug)` on both `sqcdp_metrics` and `sqcdp_problems` (DEFERRABLE INITIALLY DEFERRED, `ON DELETE RESTRICT`). The SQCDP grid + skeleton retire the hardcoded 5+3 split for a dynamic flex-weight chain (`resolveGridSizing` in `lib/grid-sizing.ts`, primary tier 1.5× secondary, clamped at 6 cols / flex-8). New curated allowlist of ~43 Tabler icons keeps the bundle bounded. `BUILTIN_CATEGORY_SEED` mirror in `lib/categories.ts` keeps frontend defaults in sync with the migration's seed (unit-tested for parity). Companion ADR [[ADR-SQCDP-Category-Schema]]. Pattern update [[TV-Viewport-Fit-Grid]] § Dynamic counts.
- [[Implement-Production-Boards-Post-Composer]] — 2026-05-17 — unified the four Production Boards content editors (Announcements / HR News / Jobs / Safety Alerts) on a single resizable `<PostComposerDialog>` with side-by-side preview / 4-tab body (Details / Media / Schedule / Audience) / drag-drop attachment uploader (dnd-kit reorder, image + PDF, per-file caption) / DateTimePicker for publish + expire / draft ↔ published toggle / curator-controlled priority (low/normal/high/pinned) / per-kind sections (announcement marquee + CTA, HR news author + category, job pay range + apply route, safety hazard + corrective action) / kind-keyed localStorage size persistence / templates dropdown / dirty-exit gate / ⌘S submit + ⌘P preview-toggle shortcuts. Migration 305 extends both `production_board_posts` and `production_board_job_postings` additively with `is_published / priority / attachments / kind_data` (posts also gain `reprompt_interval_minutes` for safety alert ack timers) + two `security_invoker` views (`v_active_board_posts`, `v_active_board_jobs`); existing `production-board-images` storage bucket extended to accept PDFs + 10 MiB limit. `useBoardPosts` + `useJobPostings` extended to round-trip the new fields. Legacy `<PostEditorSheet>` + `<JobEditorSheet>` deleted. Schema decisions captured in [[ADR-Board-Posts-Schema-Extension]]; recipe distilled as [[Production-Boards-Post-Composer]] (third sibling to [[Editable-Board-Sheets]] / [[Editable-Board-Dialogs]] — for 3+ related kinds whose shared shape dominates). 242/242 board unit tests green (20 new in `composer-types.test.ts` + 7 new in `post-composer-dialog.test.tsx`); ESLint + tsc clean.
- [[Investigate-Kit-Productivity-Not-Showing]] — 2026-05-18 — Debug note for the "I don't see my kit work" report following [[Productivity-Wiring-Kit-Workflow-Stages]]. Live RPC probes (impersonating Jai via `set_config('request.jwt.claims', …)` to bypass the MCP service-role limitation that blocked the original verifier) confirmed both `get_team_activity_events` and `get_team_productivity_counts` correctly return 31 kit_picking + 31 kit_building + 1 kit_dock_staging for Jai in the May 17 EDT window. Root cause: My Productivity / Shift Productivity default `selectedDate` to today (`new Date()`); the user's work fell into yesterday's EDT bucket so the today view (correctly) showed nothing. User must navigate to "Previous Day" to see the events. Same pass shipped a small cosmetic fix in `src/components/my-productivity-dashboard.tsx` — extended `TASK_COLORS` to include `lime`/`cyan`/`fuchsia`/`sky` so the four new kit chips render with their intended colour instead of falling through to an unstyled wrapper (the Tailwind v4 scanner needs the literal strings in the map). H1–H8 in the brief all ruled out as code bugs; system is functioning as designed.
- [[Fix-SQCDP-Metric-Update-RLS-Coerce-Error]] — 2026-05-18 — Non-superadmin curators (specifically `tka_supervisors` users whose legacy `user_profiles.role` enum was `'tka_associate'`, not the migration-295 placeholder `'viewer'`) hitting **Save** on a SQCDP metric saw `Failed to update metric: Cannot coerce the result to a single JSON object`. Diagnosed via four phases (read every RLS policy on `sqcdp_metrics` / `sqcdp_metric_history` / `production_board_sqcdp_categories`; then `pg_get_functiondef` on `has_permission(text, text)`; then a live-data audit of `role_permissions` for `production_boards.edit`; then a four-persona reproduction under `SET LOCAL ROLE authenticated` + `set_config('request.jwt.claims', …)` for Galvez / Brumbaugh / Jai / Anayeli — exact disagreement matrix between JS gate and SQL gate). Root cause: `public.has_permission(text, text)` was joining `role_permissions` on the **legacy `user_role` enum** column (`rp.role = get_user_role()`) instead of `rp.role_id`. Migration 295 used `'viewer'::user_role` as a placeholder for the custom `tka_supervisors` role's `rp.role` value; for tka_supervisors users whose `user_profiles.role` didn't match that placeholder, the enum-keyed lookup missed → `has_permission` returned FALSE → RLS denied the UPDATE → UPDATE affected 0 rows → PostgREST `.single()` raised PGRST116. The frontend `authService.checkPermission` (used by `useCanEditBoards`) JOINs on `role_id` so it correctly approved both cohorts — hence the JS gate opened the editor while the SQL gate slammed the door at save time. Fix: `supabase/migrations/308_fix_has_permission_role_id.sql` rewrites the two-arg function to JOIN through `user_profiles.role_id → role_permissions.role_id`, mirroring `authService.checkPermission` exactly. Single-function patch benefits every RLS policy that already calls `has_permission(text, text)` — `sqcdp_metrics`, `sqcdp_metric_history`, `sqcdp_problems`, `production_boards`, `branches`, `production_board_posts`, `production_board_post_acks`, `production_board_job_postings`, `production_board_sqcdp_categories`, `production_board_card_layouts`, plus the `production-board-images` storage bucket. Intentional side effect: closes a latent over-grant where pure `viewer` users were accidentally approved by the legacy-enum collision (their JS gate already correctly hid the editor so net regression = zero). Paired client-side defence: `use-sqcdp-metrics.ts` swaps `.single()` → `.maybeSingle()` on both updateMetric + createMetric and throws a permission-flavoured error when `data === null`, so a future RLS misalignment can't resurface the raw PGRST116 string. New regression test `use-sqcdp-metrics.test.tsx` pins the friendly-error contract (no `"cannot coerce"`, contains `production_boards:edit`, contains `administrator`) and the Supabase-error pass-through path. 166/166 vitest pass, eslint + tsc + build clean, no NEW Supabase advisor findings.
- [[Fix-Activity-Timeline-Missing-Kit-Events]] — 2026-05-18 — Follow-up to [[Investigate-Kit-Productivity-Not-Showing]] after the user confirmed the date-bucket fix landed the COUNTS surface (header tile / Tasks-by-Area chips) but the ACTIVITY TIMELINE Gantt strip still rendered as one big grey idle bar with `Work 0m · Idle 9h` for Jai on May 17 EDT despite 63 events. Root cause: shift-clamp loop in `buildDailyTimeline` (`src/lib/supabase/team-performance.service.ts`) silently dropped any block whose `[start, end]` was entirely outside the operator's scheduled `inline_shift_schedule` window — Jai's kit work at 20:40–22:19 EDT was 100% outside his 08:00–17:00 declared shift. Fix keeps work blocks (`taskCount > 0`, not idle/break) at their original (unclamped) bounds when off-shift instead of returning `null`, and tightens the idle-fill rebalance to `if (difference > 0)` so we never subtract idle to mask off-shift work. Operators within-shift see no behaviour change. One file touched, ~110 bytes added to `feature-shift-productivity` chunk (477.64 → 477.75 KB). 437/438 vitest pass (1 pre-existing date-bomb), tsc + eslint + build all clean. No SQL / migration changes — bug was 100% client-side post-pagination.
- [[Productivity-Wiring-Kit-Workflow-Stages]] — 2026-05-17 night — Wired the four Kitting Apps workflow stages (Picking, Building, Inspection, Dock Staging) into the productivity counters that drive **My Productivity**, **Shift Productivity**, and **Production Boards**. Migration 310 adds four new UNION arms to `get_team_activity_events` and four new count CTEs + return columns to `get_team_productivity_counts` (`kit_picking`, `kit_building`, `kit_inspection`, `kit_dock_staging`); per-line stages (picking, building) emit one event per TO line / material kitted; per-kit stages (inspection, dock_staging) DISTINCT on `kit_serial_number` because the kit-level UPDATE stamps every TO-line row. Every arm filters on its **action timestamp** (`*_date_time`, never `created_at`) — same convention [[Investigate-Shift-Productivity-Cycle-Counts-Hidden]] / migration 304 established. Org scope comes from the existing `active_users` CTE (RR_Kitting_DATA has no `organization_id` column). **Inspection-bypass exclusion** — the skip-inspection branch in `completeKitBuild` auto-stamps the inspection columns to preserve the audit trail (see [[Optional-Kit-Inspection-Toggle]]); the inspection arm gates on `NOT EXISTS (SELECT 1 FROM kitting_workflow_settings WHERE org=p_org AND kit_inspection_required = FALSE)` so orgs with inspection turned off return zero inspection events, while orgs without a settings row default-true to legacy behaviour. Frontend extensions: `ProductivityStats` / `TaskBreakdownByArea` / `TeamProductivityStats` / `AreaPerformance.taskMetrics` / `StandardActivityType` all gained four kit fields; `team-performance.service.ts` builder, `calculateTaskBreakdown` switch, `calculateAssociateEfficiency` aliases, `calculateLaborStandardComparisons`, `calculateTeamStats`, `aggregateByArea`, `getTeamProductivityForDateRange` aggregation, `mergeTaskBreakdowns`, and `exportToCsv` all extended; `MyTaskBreakdown` + `TaskBreakdownCard` (My Productivity), `associate-performance-row.tsx` legacy strip + area pills (Shift Productivity), `TASK_METRIC_CONFIG` (area card), `use-activity-config.ts` `DEFAULT_COLORS` (Gantt + timeline defaults), and `hour-bucket.ts` `targetKeyForEventType` (Production Boards — `kit_picking → picking` target; other three fall back to `default`). No new permission key (`shift_productivity:view` preserved). No new Realtime channels (TanStack Query polling unchanged). 437/438 vitest pass (1 pre-existing date-bomb failure, out-of-scope). `feature-shift-productivity` chunk +2.98 KB (477.64 KB raw). Migration applied to `wncpqxwmbxjgxvrpcake` via Supabase MCP and verified via smoke query: 55 kit_picking + 55 kit_building + 1 kit_dock_staging events in the last 30 days on j.AI OneBox; kit_inspection correctly returns 0 because the org has inspection turned off.


## Misc / cross-cutting

- [[Add-Weather-Tab-Facility-Security]].
- [[Close-Out-RF-Workflow-Future-Work]].
- [[Prune-Sidebar-Nav-Placeholders]].
- [[Zone-Engine-Hardening-Pass-Migration-233]].
- [[Outbound-Stat-Card-Filter-Wiring]] — Outbound Apps → Delivery Status and Outbound Apps → Data Manager stat cards converted to clickable filter palettes, mirroring the Inventory Counts tab. Establishes the [[Stat-Card-Clickable-Filter-Pills]] pattern.
- [[Implement-Responsive-Resize-Sweep-2026-05-16]] — 2026-05-16 — parallel-agent frontend sweep that introduces `<StatTile>` + `<KpiGrid>` (`src/components/ui/{stat-tile,kpi-grid}.tsx`), `<ResponsiveDialog>` (`src/components/ui/responsive-dialog.tsx`), the `useContainerWidth` hook (`src/hooks/use-container-width.ts`), `min-w-0` thread-through on the layout shell + table wrapper, and `collisionPadding={8}` defaults on the popover / tooltip / dropdown-menu shadcn primitives. Bans `min-w-[Npx]` on `DialogContent`. Sanctioned container-query tokens captured in [[ADR-Container-Query-Stat-Tiles]]; user-facing primitive docs in [[Responsive-StatTile-And-KpiGrid]] + [[ResponsiveDialog-Width-Tokens]]. Fixes the `/apps/inventory` Inventory Counts "Variance 25074" clipping bug. `omni_agent_v2/gui` sweep deferred (separate Tauri/Vite project, doesn't share `src/components/ui/`).
- [[Implement-Sidebar-Pin-Lock-Feature]] — pin/lock sidebar sections (nav groups + OnlineUsersPanel) per user (2026-05-21)



## Related

- [[Architecture]] — system overview.
- [[Components]] — component index.
- [[Patterns]] — pattern index (the *why* behind these implementations).
- [[Sessions]] — chronological log.

- [[Live-Verification-Realtime-Sprint-2026-05-06]] — Full live verification pass against production for migrations 270–275, 6 PgListeners, 4 REST route groups, and Tier 2 surfaces (Notifications panel, Entity-focus pill, BroadcastDialog). Verdict GREEN; 3 yellow flags (Option 2 deployed-not-active, WS token bypass, missing git SHA in build-info).


- [[Implement-Rust-Work-Service-Phase9]] — server-side trigger DSL evaluator. New `agent_triggers` table (migration 281) + `rust-work-service::triggers` module (config / dsl / loader / evaluator). Deletes `use-agent-trigger-runtime.ts` (~700 LOC) and `agent.py::_HARDCODED_TRIGGERS` + backfill poller (~750 LOC). New ADR [[ADR-Trigger-DSL-Evaluator-Phase9]].
- [[Implement-Rust-Work-Service-Phase10]] — agent identity v2. New `agent_service_keys` table (migration 283) with Argon2id hashes (no plaintext storage); 4 routes (`/register` admin-only mints `omni_sk_*` once, `/exchange` public swaps key for 15-min `kind: "agent"` JWT, `/revoke` admin sets `revoked_at`, `/list` admin reads). New `crate::agent_jwt` module + `AuthIdentity` enum in middleware with 60s Redis-cached revocation gate. Agent `_work_service_request` + WS subscribe-token mint prefer the agent JWT over the legacy user JWT. New "Agent Setup" admin tab. Backward-compat preserved — Phase 11 owns the cleanup. New ADR [[ADR-Agent-Identity-V2-Phase10]].
- [[Implement-Rust-Work-Service-Phase11]] — **FINAL phase** (2026-05-07). Architecture-change boundary release: bumps `AGENT_VERSION` 1.9.0 → **2.0.0**. Pure deletion + tightening, no new features. Migration 284 flips `rf_putaway_operations` REPLICA IDENTITY FULL → DEFAULT (Realtime is no longer the agent's row-event source for that table). Default-flips three Phase-4/7/6 env vars (`OMNIFRAME_AGENT_USE_RUST_WS`, `OMNIFRAME_AGENT_CLAIM_VIA_RUST`, `OMNIFRAME_AGENT_CONSOLE_RELAY`) from `0` → `1`; setting them back to `0` logs a deprecation warning. Deletes the legacy direct-PostgREST job-control fallback bodies (`_patch_job`, `_patch_job_terminal`, the `_CLAIM_VIA_RUST=False` branches in /jobs/claim, /complete, /fail, lease-bump). New env var `OMNIFRAME_AGENT_REQUIRE_SERVICE_KEY=1` upgrades the missing-service-key boot warning to a hard-fail exit-78. Bumps Rust bb8 Redis pool `max_size 10 → 50, min_idle Some(5)`. New `agent-2.0-architecture` capability. Surviving direct-Supabase surface (auth + domain mutations + registry + audit) documented as intentional. New ADR [[ADR-Agent-2.0.0-Release]] + new component overview [[Components/Rust-Work-Service]] + cross-phase summary [[Implement-Rust-Work-Service-Full-Integration-Summary]]. 146 cargo tests passing, pnpm build clean.
- [[Fix-Trigger-Evaluator-Empty-After-v041-Restart]] — 2026-05-14 PM — Sibling fix to [[Implement-Resilient-PgListener]] but on the INITIAL-LOAD path the listener wrapper doesn't cover. v0.1.41's `trigger_loader::run` got `Err(PoolTimedOut)` on its first `reload()` at boot (10s `acquire_timeout` lost a 13-listener boot acquire race), logged a single `WARN`, and silently proceeded with `TriggerSet::default()` (empty). Every `rf_putaway_operation_changed` NOTIFY since 14:13:03Z was dropped at the `for_table(table) { Some(_) => …, None => return Ok(()) }` branch in `triggers::evaluator::handle_notification`. Production unwedged via an admin `UPDATE agent_triggers SET updated_at = now() WHERE id = 'b8160159-…'` (Step 1; fired a fresh `agent_triggers_changed` NOTIFY → loader's LISTEN-driven retry reloaded the rule set in 344 ms). 35-row backlog drained in ~14 min via the existing pg_cron `*/5` orphan-replay path (migration 289) feeding the now-armed evaluator; 62 jobs completed clean, 0 failed. Permanent fix: v0.1.42 `rust-work-service/src/triggers/loader.rs` wraps the initial reload in a bounded retry loop (`INITIAL_RELOAD_MAX_ATTEMPTS = 5`, exponential backoff 1s/2s/4s/8s, `error!` ladder at giveup, falls back to LISTEN safety net). 4 new unit tests gated on an injected backoff fixture so they run in <1 ms each (no `tokio` `test-util` dependency added). Quality gates clean: `cargo build` clean, `cargo test` 166/166, `cargo clippy` 0 new warnings, `pnpm lint:check` 0 new errors. **Deploy attempt FAILED** — v0.1.42 deployment `f62ad870-…` crash-looped 10× on `src/main.rs:301:6` (listener pool's eager `connect_with` panicking with `PoolTimedOut` against the OLD v0.1.41 container's Supavisor session-mode hold), same class as the AM's v0.1.40 failure on the general pool but on a different line. Production stays on v0.1.41; user must approve extending the AM's lazy-pool pattern to the listener pool to unstick the deploy. The retry-loop fix is sitting on local HEAD.
- [[Implement-Resilient-PgListener]] — 2026-05-07 PM — Resilient `sqlx::PgListener` wrapper in `rust-work-service`. Permanently fixes Bug 3 of [[Debug/Fix-Auto-Confirm-Putaways-Trigger-Missing-And-Listener-Wedge]] (silent TCP socket death between Railway and Supabase pgbouncer left ~8 of 13 boot-spawned listeners wedged with no `recv() failed` log lines). New `rust-work-service/src/pglistener.rs` exports `pglistener::run` — every listener LISTENs on its real channel AND on a shared `rust_work_service_keepalive` channel; publishes its own keepalive every 30 s via the main `PgPool` (separate connection from the dedicated PgListener socket); force-reconnects when no frame arrives within 90 s. Refactors all 10 existing listeners (`settings`, `sap_agents`, `sap_jobs`, `sap_import_runs`, `cycle_count`, `lx03`, `rf_putaway`, `notifications`, `triggers/loader`, `triggers/evaluator`) to drop their hand-rolled reconnect loops. 5 new Prometheus metrics (`work_pglistener_status`, `_reconnects_total`, `_last_message_age_seconds`, `_keepalive_sent_total`, `_keepalive_received_total`). 6 new unit tests (mpsc-mocked `NotifySource` + `KeepaliveSink` exercise watchdog + recv-error reconnect — no live PG required). Deployed via `railway up` (image `sha256:d4a59402480ce49b…`); post-deploy `pg_stat_activity` shows all 13 listener backends alive; `/metrics` confirms 13 senders × 3 echoes per channel. DB schema untouched.
- [[Implement-Rust-Work-Service-Full-Integration-Summary]] — **TOP-LEVEL SUMMARY** of all 12 phases (0-11) of the comprehensive plan. Architecture before/after, per-phase scope table, total LOC delta (~+10,000 Rust + ~+850 agent net + ~+800 FE net + ~+1,200 typed clients + 8 migrations 271-284), performance metrics (job dispatch latency 60-180s → 1-3s, Realtime cycle 12+/min → stable single socket), open follow-ups deferred to v2.1.0+.
- [[Implement-Phase10-Service-Key-First-Rollout]] — 2026-05-07 EOD cleanup (Workstream A item 6) — operational runbook for the first end-to-end Phase 10 service-key registration. Step-by-step admin recipe (open Setup tab → register agent → save key to `%USERPROFILE%\.omniframe\agent_service_key.txt` → restart agent → verify boot banner posture → confirm `last_used_at` ticks). Three rollout checkpoints (first agent live → 50% fleet + `OMNIFRAME_AGENT_REQUIRE_SERVICE_KEY=1` for new installs → 100% + schedule v2.1.0 user-JWT removal). Failure-recovery matrix for the common gotchas (lost plaintext modal, wrong agent_id, NOT CONFIGURED despite step 3 looking right, rate-limit thrash, etc.).
- [[Implement-Resilient-Work-Service-WS-Client]] — 2026-05-11 — Mirrors the rust-side [[Implement-Resilient-PgListener]] keepalive + watchdog pattern onto the Python agent's `omni_agent/work_service_ws.py`. Closes the May 7 PgListener resilience audit's open follow-up. User symptom that motivated the work: agent's console in Citrix appears frozen / disconnected from the browser until the user "presses something" — Citrix VDA + corp proxy (Netskope/ZScaler) was silently idle-closing the WS socket and leaving a half-open TCP that the `websockets` library couldn't see. Two-layer defense: (1) `websockets.connect(ping_interval=20s, ping_timeout=10s)` for protocol-level pings; (2) sibling asyncio `_watchdog_loop` task wakes every 15s and force-closes via `ws.close(code=1011)` if `_last_message_received_at` gap exceeds 60s. Reconnect ladder retightened to bounded exponential `1s → 30s` (matches `RECONNECT_BACKOFF_MAX`). Tightened exception classes (`OSError`, `asyncio.TimeoutError` explicit; watchdog-driven closes rebadged from `ConnectionClosedError` to `WatchdogTimeout`). 4 new public methods (`reconnect_count`, `watchdog_trips`, `last_message_received_at`, `last_reconnect_reason`) ready for a future `/health` JSON extension. 4 env-tunable knobs with sensible defaults. Module-level WARN if `watchdog_timeout < 2 × ping_interval`. Boot banner one-liner. New reconnect log shape `[work-ws] reconnect #N (last message Xs ago, reason=<class>, watchdog_trips=M); sleeping <backoff>s`. Verified with two non-network smoke tests (sad path: 3 reconnects + 3 watchdog trips in 5.5s, all `WatchdogTimeout`; happy path: 0 reconnects + 9 events delivered in 4s). NO `AGENT_VERSION` bump (still 2.0.0). NO message-protocol changes. NO changes to `agent.py` or `rust-work-service`. AST + flake8 clean for source + MacWindowsBridge mirror; `cmp` byte-identical. Worst-case agent recovery time bounded at ~75s on defaults.
- [[Implement-Putaway-Confirm-Backfill-Loop]] — 2026-05-08 (rust-work-service v0.1.34 → 0.1.35) — closes the auto-recovery gap that left putaway TO confirms permanently `failed` whenever the agent's SAP GUI session had a transient hiccup. Pairs the listener-keepalive transport guarantee in [[Implement-Resilient-PgListener]] with a data-plane recovery loop. Migration 289 ships `public.backfill_pending_putaway_confirms(...)` (pg_cron `*/5 * * * *`, jobname `omniframe-backfill-pending-putaway-confirms`) with two branches — requeue stale `failed` jobs (clear claim/heartbeat fields, `attempts=0`, raise `max_attempts`, breadcrumb-append the `error`) and replay NOTIFYs for true orphans. Trigger UUIDs join through `agent_triggers` so future tenants light up automatically. New Rust route `POST /api/v1/sap-agents/backfill-pending-confirms` (org-scoped to caller's JWT) is the on-demand escape hatch. New `useStuckPutawayConfirms` hook + admin-only "Force backfill now" button on the Pending Confirms stats card. Live verification 23:50–23:54 UTC drained all 11 originally-stuck rows on org `c9d89a74` (oldest 477 min) within 4 minutes; post-drain `still_pending_24h=0`.



- [[Re-Enable-CurrentPage-In-ActiveOperators]] — 2026-05-07 — FE-only sprint that partially restores `current_page` on the presence payload, scoped to the Active Operators panel inside the Inventory Counts tab. Realises [[ADR-Scoped-CurrentPage-In-ActiveOperators]]. Step 1 verification confirmed the Rust pass-through is transparent (`#[serde(flatten)]` on the heartbeat route + loose `serde_json::Value` payload everywhere downstream), so no Rust release was required. Touches: `types.ts`, both presence service classes (`updateCurrentPage` re-added with `scheduleTrack`/`scheduleHeartbeat` debouncing + idempotence), new `route-features.ts` resolver module (~50 entries), `usePresenceTracker` route effect restored, `<LiveOperatorStatus>` cross-references `useActiveWorkers()` against `usePresenceOptional()` for the per-worker `current_page` lookup.
- [[Implement-LiveOperatorStatus-InBuilding-Tab]] — 2026-05-07 — Same-day FE-only follow-up to the entry above. Adds a second tab ("In Building") to `<LiveOperatorStatus>` showing the **Option-2 union** of presence-tracked users that are NOT currently checked in to the work engine. Tab 1 ("On Counts") stays as-is — work-engine operators from `worker_heartbeats`. Tab 2 dedup: `inBuildingUsers = allPresent.filter(u => !workerIds.has(u.user_id))` (Set lookup). Compact cards (~60px row, `<PresenceAvatar size="sm">`, single-row layout) reuse `resolveFeature()` for navigation labels and inherit the same RBAC gate (no new permission key). Step 4 decision: **Option A — per-tab status counts strip semantics** (Tab 1 = 5 work-engine tiles; Tab 2 = 4 presence tiles via new `PRESENCE_THEME` palette). DND folded into Busy on the strip; avatar dot still shows precise DND colour. Privacy contract intact — same 8 files match `current_page` in `src/`. Flagged: kiosk opt-out gap (RF terminals on idle screens still invisible in Tab 2 due to `PRESENCE_KIOSK_ROUTE_PATTERNS`); now safe to revisit since Layer 7 ([[Implement-Presence-On-Rust-Option-2]]) shipped.


- [[Implement-Inventory-Adjustment-Workflow]] — 2026-05-07 — New end-to-end workflow on the SAP Testing → Inventory Management tab. LT10 row action (`+ Add to Inv. Adjust`, gated on capability `zmm60-price-lookup`) calls a new agent endpoint `POST /sap/zmm60/lookup` (handler in `omni_agent/zmm60_lookup.py`, mounted via the same lazy `app.include_router` pattern as `material_master_read.py`); browser INSERTs into `public.inventory_adjustment_staging` (migration 288, with `extended_value` as a STORED generated column + `zmm60_raw JSONB`); new `inventory-adjustment` `kind: 'tool'` Library entry renders `<InventoryAdjustmentView />` with three stat cards (Net Value / Gross Gains / Gross Losses), per-row Remove + confirm dialog, and a lazy-imported `exceljs` Excel export (`inventory_adjustment_YYYYMMDD_HHmm.xlsx`). No Realtime channel — see [[realtime-policy]]; TanStack Query invalidation on `['inventory-adjustment-staging', orgId]` covers refresh. AGENT_VERSION stays at 2.0.0 (additive). `feature-admin-sap` chunk: 446 KB / 120 KB gzip (+19 KB). Excel export uses already-installed `exceljs` lazy-imported on click — no new dependency.



- [[Implement-RF-Activity-Telemetry-In-LiveOperatorStatus]] — 2026-05-07 evening — Third presence-evolution sprint of the day. Extends the supervisor's `<LiveOperatorStatus>` panel with granular RF activity telemetry (current workflow step, last scan, idle indicator, work task / zone). New nested `rf_activity?: PresenceRfActivity \| null` block on `PresencePayload` + `PresenceUser`; same single-consumer privacy contract as `current_page` (`<LiveOperatorStatus>` only, RBAC-gated by `view inventory_apps`) — see [[ADR-RF-Activity-Telemetry]]. New `useRfPresenceActivity` hook mounted once in `<RFInterface>` watches the parent's `currentView` / `currentTask` / `currentZone` plus document-level capture-phase keydown + pointerdown listeners (delegated on `data-slot="scanner-input"` for scan capture). Tab 1 cards get an `<RfActivityRow>` sub-row (step · last scan · live pulse / idle badge); Tab 2 compact cards get an inline `<RfActivityIndicator>` (radar icon + tooltip snapshot — keeps card height unchanged). Visual freshness ladder (`< 10s = live`, `10–60s = recent`, `> 60s = idle`) recomputed locally on a 5s `useNowTicker` to avoid re-broadcasting on every keystroke. Kiosk opt-out narrowed: `PRESENCE_KIOSK_ROUTE_PATTERNS` from `/^\/rf-/` (entire RF tree) to `/^\/rf-signin(\/|$)/` (sign-in only) — Layer 7 (Option 2 — server-side Rust presence) retired the load argument that drove the original opt-out. `<RFLayout>` enrolled in `<PresenceProvider>` to fix RF being OUTSIDE `_authenticated`. **No Rust changes** — Worker 1's `serde_json::Value` payload design carries the new nested object verbatim (second FE field-shape extension this week to ride that investment). Phase 0 finding: RF tree has no central state machine; v1 publishes work-type-level granularity, per-form sub-step granularity is opt-in future work — the `STEP_LABELS` lookup pre-includes future sub-step labels (`scanning_material`, `confirming_count`, etc.) so a per-form integration drops in cleanly.



- [[Implement-Inventory-Mgmt-Detail-Pane-Redesign]] — 2026-05-09 — Round 5 polish on the SAP Testing → Inventory Management tab. Lifts the active query Form Card out of the round-4 left rail (where it was stacked below the Library) and into the right pane as a unified **Query Detail Card** that composes identity → description → status row → inputs → Run CTA → Batch panel in one scannable column. Library shrinks to a 320px sticky left rail. The agent-offline message moves out of the empty-state Card and into an always-visible status strip on the Detail card. "Transfer Inventory (LT01)" demoted from a `w-full` row to an `sm` outline chip alongside Run + Batch Mode. `ResultsEmptyState` compacted from a 400px Card with duplicate identity to a 64px dashed-border placeholder row. Inputs grid widens to `lg:grid-cols-3`. Inventory-adjustment tool branch column aligned to 320px for visual consistency. Pure layout/UX restructure — no handler logic, capability gate, persistence key, dialog, agent rebuild, migration, or `LATEST_AGENT_VERSION` bump. Single file touched (`src/features/admin/sap-testing/components/inventory-management-tab.tsx`). `pnpm exec tsc -b` clean; ESLint clean; repo-wide warning count unchanged.



- [[Implement-Operator-Cycle-Count-Queue-Tab]] — 2026-05-09 — Per-operator cycle-count task queue on `<LiveOperatorStatus>`. **AM ship**: third "Up Next" peer tab (operator dropdown + drag-to-reorder list of the next 12 tasks). **PM refactor (same day)**: tab swapped for a **click-to-open dialog** — each `<OperatorCard>` on Tab 1 ("On Counts") is now `role="button"` and opens `<OperatorTaskQueueDialog>` scoped to that operator. Tab state union narrowed back to `'on-counts' \| 'in-building'`; operator dropdown dropped (operator implied by the clicked card). Animation: shadcn `<DialogContent>`'s built-in `fade-in-0 zoom-in-95 duration-200` for the wrapper + framer-motion `<motion.ul>` / `<motion.li>` for the inner list stagger (`staggerChildren: 0.025`, honours `prefers-reduced-motion`). The dialog body mounts only while the dialog is open, so the WS subscription registered by `useWorkerTasks(..., { enableRealtime: true })` is cleaned up on close (strictly less work than the always-mounted tab body). Uses the existing `GET /api/v1/workers/:id/tasks` Rust route + `workServiceWs` singleton (no new `supabase.channel(...)` callsite — honours [[realtime-policy]]). Drag via `@dnd-kit/core` + `@dnd-kit/sortable`. `useOperatorTaskQueueOrder` hook + pure `mergeOrder` helper untouched (12 unit tests still pass). [[ADR-Supervisor-Task-Queue-Reorder-Persistence]] untouched — the persistence decision is independent of the surface. Net `inventory` chunk delta across both same-day passes vs. HEAD-before-AM-ship: roughly +13 KB / +3 KB gzip; refactor pass alone was −0.79 KB raw / +0.09 KB gzip. No migration, no Rust release, no `LATEST_AGENT_VERSION` bump.



- [[Implement-Inventory-Management-Fleet-Routing]] — 2026-05-09 (round 6) — Tab-level Local / Fleet routing toggle on the SAP Testing → Inventory Management surface. Generalises the SmartImportButton's LT22 fleet-routing pattern ([[Patterns/Fleet-Aware-Smart-Routing]]) to the WHOLE tab — every in-scope action (LT10/MB52/MMBE queries, ZMM60 lookup, LT01 transfer, LS02N bin blocks, MM02 mutations via Phase 5, LS01N create-bin) flips between `agentFetch('localhost:8765/...')` (today) and `sap_agent_jobs` queue claim-pinned to a picked fleet agent (new). New hook `useExecutionMode()` owns localStorage persistence + capability validation against the live `useAgentDetection().fleet.agents` snapshot + the single `dispatch(endpoint, payload, opts)` entry-point that routes BOTH paths to the same `NormalizedDispatchResult<T>` envelope so call sites don't branch. New toggle UI `<InventoryExecutionModeToggle />` renders below `<AgentStatusBar />` — segmented Local | Fleet button group + agent picker (when fleet) + per-action capability warning. SAP Recorder + Reversal Engine bypass the toggle entirely (need LIVE local SAP GUI session); Library entries gain a `Local-only` pill + 50% opacity in fleet mode + `<LocalOnlyToolBanner />` in the right pane. Agent registration delta: one-line `_JOB_ENDPOINT_MODELS["/sap/zmm60/lookup"] = Zmm60LookupRequest` in `omni_agent/agent.py` so queue dispatch can instantiate the Pydantic model on a queue-claimed ZMM60 job. **No `AGENT_VERSION` bump** (stays 2.0.0), no new `supabase.channel(...)` callsite, no Rust release, no migration. `feature-admin-sap` chunk +3.05 KB raw / +1.02 KB gzip.



- [[Implement-Work-Queue-Management-Tab]] — 2026-05-10 — New "Work Queue Management" tab on Inventory Management between **Inventory Counts** and **Operation Control**. Live multi-operator dispatcher: one column per active operator, each column showing that operator's NOW (hero card) above their NEXT pipeline (slim rows). Tasks animate in / out via framer-motion with `<MotionConfig reducedMotion="user">` honouring OS prefer-reduced-motion automatically. Within-lane reorder via the SAME `useOperatorTaskQueueOrder` hook the per-operator dialog uses (cross-surface localStorage sync added — the hook now listens for `storage` events). Cross-lane reassign via drag (or keyboard: Space → ArrowLeft/Right → Space) calls `workServiceClient.pushToUser` with optimistic UI + sonner Undo toast (8s window). In-progress tasks are drag-disabled with a `cursor-not-allowed` grip. Realtime via the existing `workServiceWs` singleton with **one shell-level handler for the whole grid** rather than one per lane (see [[ADR-Dispatcher-Shell-WS-Handler]]); the dialog's `useWorkerTasks` continues to subscribe per-instance, sharing the `WORKER_TASKS_QUERY_KEY` cache. Virtualisation via `@tanstack/react-virtual` kicks in when per-lane task count >12 OR total visible >110. Ghost lanes for operators who just went offline (35% opacity + grayscale, 6s TTL). Honours [[realtime-policy]] — zero new `supabase.channel(...)` callsites. Zero new dependencies. New SQL migration `294_seed_work_queue_management_tab.sql` seeds `tab_definitions` + `role_tab_permissions` for the new tab — not auto-applied; admin/superadmin see the tab immediately via the role bypass. `pnpm build` clean: `inventory` chunk **218.05 KB raw / 51.25 KB gzip** (well under the 500 KB limit). 19/19 tests pass (4 multi-operator + 3 cross-lane + 12 reorder regression).


- [[Fix-Kit-Build-Cross-Linked-Parts]] — 2026-05-12 — Resolution shipped (afternoon, session 2). Multi-kit-per-PO cross-link bug closed end-to-end: forward migration `303_kit_build_flags_serial_scope.sql` (per-serial unique-active flag rule + composite indexes), service-layer rescoping across `kit-kanban.service.ts` / `rf-kitting-picking.service.ts` / `rr-kitting-data.service.ts` (every kit-level mutation now keys by `kit_serial_number`; PO-scoped variants kept as deprecated fan-out wrappers), in-app `kit_select` picker on `RFKittingPickingForm` for the multi-kit case, append-TOs disambiguation in `KittingDataManager`, 6 new regression tests, and a live data-cleanup pass for the KIT-001 / KIT-002 pair (kanban link restored, per-kit progress recomputed: 27/31 and 8/18). New pattern note [[Kit-Serial-Scoping]] documents the forward-looking convention ("key by `kit_serial_number` for any per-kit mutation; reserve `kit_po_number` for PO-group operations"). No commit yet — staged for user review.


### Multi-Session Agent Master (planning)

- [[Plan-Multi-Session-Agent-Master]] (2026-05-14) — 12-section planning doc for a Master Controller GUI that supervises 6 worker processes, each pinned to one SAP GUI session. Recommends **CustomTkinter** (zero new native deps, trivial PyInstaller), **static session assignment** (reuses v1.7.9 pinning), **per-worker service keys** under `%USERPROFILE%\.omniframe\agents\W<N>\` (reuses Phase 10 loader). Worker side gets a thin Phase A hardening (`/admin/ws/reconnect`, `/admin/job/abort`, `/admin/sap/reattach`, `/health` extended with `ws_connected`/`sap_attached`/`job_age_seconds`/`identity_status`, env-driven `OMNIFRAME_AGENT_PORT` + `OMNIFRAME_AGENT_SELF_ID_OVERRIDE` + `OMNIFRAME_SAP_CONN_IDX`/`OMNIFRAME_SAP_SESS_IDX`). No rust-work-service changes; no FE changes (existing fleet card already handles N agents per [[Implement-Multi-Agent-Coordination]]). Phased rollout A→G with explicit exit criteria. Six open questions in Section 11 gated on user decision before Phase A starts.


## OmniAgent v2 (Rust shell)

- [[Build-OmniAgent-V2-Rust-Shell]] — Worker A's 6-crate scaffold under `omni_agent_v2/`. Headless agent.exe + Tauri GUI commands + JSON-RPC supervisor for the Python COM helper. v1.x Python agent stays untouched.



- [[Implement-Production-Boards-Bento-Layout]] — 2026-05-17 — Replaced the flat PostCard strip on the four secondary Production Boards (Announcements / HR News / Jobs / Safety Alerts) with a curator-editable bento mosaic: drag-to-rearrange (`@dnd-kit/core`), drag-to-resize (hand-rolled pointer events), 5 visual variants (classic / banner / gallery / spotlight / quote) with per-variant default sizes + config bag (gallery rotate interval, banner cover position). Migration 307 adds `production_board_card_layouts` (per-org, per-board, per-scope, polymorphic `post_id` + cleanup triggers, RLS gated on `production_boards:edit`). New `<BentoGrid>` + `<BentoBoardShell>` + `<CardRenderer>` + 5 variant cards + `<BoardCardVariantPicker>` (mounted inside the composer's Details tab as a new Section). Lazy-loaded `feature-production-boards-bento` chunk at 76.74 KB / 23.73 KB gzip; `feature-shift-productivity` net +3 KB (473.88 KB raw, well under the 500 KB budget). 30 test files / 343 tests passing (+62 new). Pattern: [[Bento-Grid-Layout]]. ADR: [[ADR-Production-Boards-Bento-Layout-Persistence]].

- [[Implement-Production-Boards-Aesthetic-Overhaul]] — 2026-05-17 — Design-driven v2 visual overhaul on top of the same-day bento engineering ([[Implement-Production-Boards-Bento-Layout]]). 16 new files + 12 modifications. 6 new primitives in `components/bento/`: `board-kind-accent.ts` (single per-kind gradient source of truth), `board-atmosphere.tsx` (4-layer CSS mesh + grain backdrop, pure keyframes), `board-empty-state.tsx` (per-kind hero artwork + headline + CTA, CSS-only artwork), `board-header.tsx` (eyebrow + gradient title + filters + actions cluster + hairline), `board-filter-chips.tsx` (gradient-active chip strip), `live-pulse.tsx` (universal kind-coloured pulse). 5 variant cards rebuilt with the editorial cascade (eyebrow → headline → support → meta) — banner gets 18s Ken Burns + kind-tinted 4th shadow stop on hover, gallery gets 600ms blur+opacity crossfade + glass caption + accent dot-pager, spotlight gets Apple-style icon bubble + halo + display headline, quote gets refined pull-quote (display weight italic), classic gets editorial cascade + vertical gradient stripe + hover image Ken Burns. Page header collapsed to slim h-12 inline row; tab strip dropped heavy container, became inline segmented control with per-kind active underline. All four content boards wired through `<BoardHeader>` + `<BoardAtmosphere>` + `<BoardEmptyState>` + `<BoardFilterChips>`. 375/375 tests passing (was 343 → +32 new). `feature-shift-productivity` net +0.78 KB (474.66 KB raw, under 500 KB ceiling); `feature-production-boards-bento` lazy chunk +19.97 KB (96.71 KB raw / 28.78 KB gzip). No new dependencies, no new fonts (reused page-preloaded Geist + Inter), zero new schema. Pattern: [[Patterns/Premium-Board-Aesthetic]]. ADR: [[Decisions/ADR-Production-Boards-Aesthetic-Overhaul]].



- [[Implement-SQCDP-Measured-Hero-Typography]] — 2026-05-17 — v15.2, third same-day pass on SQCDP TV hero typography. **Supersedes [[Implement-SQCDP-Uniform-Hero-Typography]] (v15.1) and [[Implement-SQCDP-Hero-Autofit-Typography]] (v15).** Both prior CSS-only attempts failed on the user's TV display — v15 (`cqh`) drifted across siblings, v15.1 (`vh`) made siblings agree but had no width awareness so longer values (`99.7%`, `73 DAYS FOR PHYSICAL`) overflowed. The user's third request flagged the v15.1 overflow + the secondary single-mode cards inheriting the primary clamp; pure-CSS approaches can't deliver "every card the same px AND no card overflows" without measuring what actually fits. v15.2 introduces `useUniformHeroFit` + `<SqcdpHeroFitProvider>` (new files in `boards/sqcdp/hooks/` + `boards/sqcdp/components/`) with three independent tier registries (`primary` / `sub` / `secondarySingle`). Compute pipeline: measure each entry's natural one-line width at refPx 100, compute `maxFit = (clientWidth - safetyPx) / natural * refPx`, take `min` across tier, clamp by viewport ceiling (`vh`-based), floor at tier lower bound, round to 4 px. Overflow fallback: snuggest entry demoted to `whitespace-normal` + `line-clamp-2`, tier picks size from survivors. Trend icon scales from measured hero px (`clamp(28, heroPx*0.5, 80)`). Curator override defers via `enabled: false`. Stable ref callback (refs-of-everything trick) so context updates don't churn registration. Shared `ResizeObserver` + debounced (100 ms) window resize + RAF-collapsed recompute + natural-width cache. First-paint `opacity-0` until `fit.ready`. Decision log + alternatives in [[ADR-SQCDP-Measured-vs-Viewport-Hero-Typography]]; pattern recipe extended into [[Patterns/TV-Viewport-Fit-Grid]]. Visual QA matrix at 1080p/1440p/4K, 161 unit tests (+5 net), bundle delta +0.78 KB.


- 2026-05-19 — [[Apply-Performance-Review-Fixes-2026-05-19]] (end-to-end execution of the P0/P1/P2 production-slowness plan; 32-connection drop, 499 RLS init-plan warnings cleared, cross-tenant LX03 leak closed, Redis re-provisioned)


- [[Enable-Bulk-Role-Change-In-User-Management]] — (2026-05-19) Bulk role picker now uses DB-driven roles, current-role distribution preview, and permissions-change warning; matches single-user `UserChangeRoleDialog`.

- [[Upgrade-Sqlx-08-All-Rust-Services]] — (2026-05-20) Coordinated upgrade of all five sqlx-using Rust services in the monorepo from `sqlx = "0.7"` → `sqlx = "=0.8.6"`. Closes RUSTSEC-2024-0363 (Postgres-protocol PoC against 0.7.x) and unlocks the Supavisor-transaction-mode-pooling foundation (PR #3863 unnamed prepared statements) that [[Fix-Sqlx07-Supavisor-Transaction-Mode-Incompatibility]] gated on. Five parallel service workstreams ran, one per service: `rust-dashboard-service` (4 queries, 65 s release build), `rust-ai-service` (5 queries, 80 s), `rust-mdm-service` (26 queries, 80 s), `rust-core-service` (dual pools + dropped unused `migrate` + `bigdecimal` features, 85 s), `rust-work-service` (95+ queries, 13-channel `PgListener` fleet, dual pools, 100 s). Zero compile errors and zero source-file changes across all five services — the audit prediction held verbatim. All `cargo test --lib` (or `cargo test` on bin-only crates) pass: 7+3+7+23+167. Per-service notes: [[Upgrade-Sqlx-08-Dashboard-Service]], [[Upgrade-Sqlx-08-Ai-Service]], [[Upgrade-Sqlx-08-Mdm-Service]], [[Upgrade-Sqlx-08-Core-Service]], [[Upgrade-Sqlx-08-Work-Service]]. Recommended deploy order (lowest risk first): dashboard → rust-ai-service → mdm → core (bake 30 min) → work-service (watch `work_pglistener_reconnects_total` for the rustls 0.21→0.23 swap and the `fetch_optional` leak fix). Nothing committed; nothing deployed.


- 2026-05-20 — [[Debug/Fix-Cycle-Count-Resolve-FOUND-Check-2026-05-20]] — Replaced `IF v_result IS NOT NULL` with `IF FOUND` in `auto_resolve_cycle_count_location()`; backfilled 17,099 poisoned `rr_cyclecount_data` rows. Re-enables `serpentine_zone` queue ordering in `rust-work-service`.


- [[Improve-Tile-Layout-And-Per-Bay-Start-Stop]] — 2026-05-21 — Master tile UX: full-word 3×2 action grid (Fix/Start/Stop/Restart/Reassign/Console), `compute_button_state` + `is_running`, `start_worker`, context menu alignment. Tests: `test_tile_button_state`, supervisor `start_worker` cases.


- [[Implement-SQCDP-Auto-Counter]] — 2026-05-24 — v16 of the SQCDP card surface. Adds a per-metric auto-counter (Days / Hours / Weeks / Months since an anchor timestamp) so cards like Safety · TBIR "861 Days" auto-increment as days pass and snap to 0 on reset. Migration 310 (`auto_value_config jsonb NOT NULL DEFAULT '{}'`). New `lib/auto-value.ts` with `computeAutoValue(cfg, now)` (now injected for tests), `isAutoValueActive` predicate, `tickIntervalFor(mode)` for the card's clock cadence, and `parseAutoValueConfig` defensive parser. `useAutoValueClock` ticks at mode-aware cadence (60s for days/hours, 5min for weeks/months), pauses on hidden tabs. Editor surface: collapsible Section 3 · Auto-counter in the Basics tab — enable toggle, unit ToggleGroup, datetime-local anchor picker, midnight-floor switch (Days mode only), `Reset counter to current moment` button, live preview pill. The static `Current value` input is disabled while the counter is on. 199/199 SQCDP tests pass (21 new pure-fn + 3 new card smoke). Pattern distilled as [[Anchor-Date-Auto-Counter]] for reuse on the next "N units since event X" surface.

- [[Implement-OmniBelt-MVP]] — 2026-05-24 — Site-wide floating tool launcher (**OmniBelt**) shipped across 10 phases (P0–P9) in a single day. P0 closes the third Rust-service gap in [[Supabase-Read-Replica-Routing]] by adding `read_pool` to `rust-dashboard-service` (single-binary crate; wired inline in `src/main.rs`). P1 lands the foundation Supabase migration 327 (3 tables `omnibelt_role_config` / `omnibelt_user_prefs` / `omnibelt_tool_events`, 24h MV `omnibelt_tool_events_24h_mv` refreshed every 5 min via `pg_cron`, NOTIFY trigger `omnibelt_role_config_notify`, 5 RLS policies, 2 BEFORE UPDATE touch triggers, new `omnibelt.manage` permission granted to `admin` + `superadmin`) plus the frontend route-gate + kill-switch scaffolding + Zustand store factory + 108 vitest cases. P2 ships the Rust bootstrap endpoint `GET /omnibelt/bootstrap` on `rust-dashboard-service` with 30s Redis cache + read_pool, the `WsEvent::OmnibeltConfigChanged` variant on `rust-work-service`, a new `omnibelt_listener` PgListener that DELs the Redis cache key pattern + broadcasts the WS event, 5 FastAPI proxy routes under `/api/omnibelt/*`, frontend `useOmnibeltBootstrap` TanStack Query hook + `useOmnibeltConfigInvalidator` WS subscriber. P3 mounts `OmniBeltHost` in `__root.tsx` between `<Outlet />` and `<Toaster />`, ships the Pill skin with tri-state collapse (Orb → Pill → Panel + edge Nub for auto-hide) via shared `framer-motion` `layoutId='omnibelt-host'` morph (Dynamic-Island primitive), the shared Panel with Pinned / All / Recent / Running tabs + search + lazy-loaded tool shells, the `feature-omnibelt` Vite chunk slice. P4 fills the 8-tool v1 roster (`quick_pick`, `sap_status`, `inventory_lookup`, `background_jobs`, `quick_note`, `build_info`, `settings_shortcut`, `help_docs`) with RBAC via `usePermissionStore`. P5 wires the Mach 3 status surface (SVG halo rings around the orb/pill, click-expand status tray with 4s autoexpand-on-own-job, `useOmnibeltJobs` aggregating 3 `workServiceWs` variants `SapJobStatusChanged`/`ImportRunStatusChanged`/`TriggerFired`, 6 OKLCH job-type color tokens). P6 lands the position system: 12 anchor zones (4 corners + 4 edges + 4 nubs), 2 modes (FREE / PINNED), drag + 32px snap deadzone, collision avoidance against `NotificationsPanel` bell + Sonner toaster, per-route-class position memory (bounded to ≤10 classes), right-click "Move to corner" menu, `translate3d` GPU-only positioning. P7 ships the Orb (radial fan-out) + SkyStrip (top-center Dynamic-Island morph) alternate skins lazy-loaded into their own Vite chunks (`feature-omnibelt-skin-orb` = 2.18 KB gzip, `feature-omnibelt-skin-skystrip` = 1.12 KB gzip) + skin picker in the Panel overflow menu. P8 mounts the dedicated admin sidebar entry under Administration + route `/admin/omnibelt` + 5-tab dashboard (Overview / Tools / Role Defaults / Analytics / Audit) with v1-rich analytics derived from `omnibelt_tool_events_24h_mv` (`feature-admin-omnibelt` = 40.63 KB gzip). P9 catches one missed migration (328 admin nav row + role visibility grants) and applies it via Supabase MCP; fixes 1 OmniBelt-introduced lint error + cleans 6 OmniBelt warnings; produces full end-to-end verification (380/380 OmniBelt tests pass, all bundle budgets under target, zero new `supabase.channel(...)` callsites, OmniBelt-scoped lint = 0 errors / 0 warnings, all 5 RLS policies + MV + trigger + cron + permission verified live via Supabase MCP). Net: ~10 new source folders under `src/features/omnibelt/` + `src/features/admin/omnibelt-dashboard/`, ~100 source + test files, ~7,500 LOC added, 2 Supabase migrations applied (327 + 328), 1 Rust service (`rust-dashboard-service`) gains `read_pool`, 1 new `rust-work-service` WS variant + PgListener, 5 new FastAPI routes, 4 new Vite chunks (all under budget). See [[ADR-OmniBelt-Site-Chrome]], [[OmniBelt-Floating-Launcher]], [[OmniBelt - Site Tool Launcher]]. ALL OmniBelt-scoped quality gates pass; pre-existing chunk-budget breaches (`warehouse-location-map` 1.5MB, `feature-admin` 1MB, `feature-rf-interface` 532KB) and lint-ratchet drift (98 vs 16 baseline) are unchanged by this work.



- 2026-05-30 — [[Implement-Mimalloc-And-Moka-Auth-Cache-Work-Service]] — First increment of [[ADR-Rust-Dependency-Modernization-Roadmap]]. mimalloc global allocator + a moka in-process JWT-validation cache inside `rust-work-service` `AuthClient` (30s TTL via `AUTH_CACHE_TTL_SECS`, `0` disables; only successes cached; `work_auth_cache_total{outcome}` metric). 4 new tests (hit-bypasses-network, errors-not-cached) + 178 unit tests pass; release LTO build clean; **~85% cache hit rate live** on v0.1.44. Also annotated (not deleted) the `observability/middleware.rs` idempotency Phase-1.5 foundation. Unblocked by [[Fix-Work-Service-Monorepo-Root-Directory-Misdeploy-2026-05-30]].

- 2026-05-30 — [[Optimize-Auth-Cache-Arc-Value-Work-Service]] — follow-up to the moka cache: store `Arc<AuthenticatedUser>` and thread the `Arc` end-to-end (cache → middleware → request extensions → 12 route files / 46 handler sites). Collapses the two deep clones per protected request (moka `get` + `Extension` extraction of the `Vec<String>` permissions/Strings) into pointer bumps; common hit path is now 0 deep clones. Org-id DB fallback uses `Arc::make_mut` (clones inner only when org missing, never leaks into the cached entry). `0.1.44 → 0.1.45`; cargo check/test/clippy(no new)/release-LTO all clean.
- [[Fix-Location-3D-HUD-Overlap-And-Contrast]] — 2026-06-08 — Location tab: de-conflict the shell's 2D-era overlays from the isometric scene's own HUD (Export hidden in 3D, compass/scale cluster → above the nav hint, FloorSwitcher → right-edge-centred, edit hint 2D-only) + scene-contrast tuning (darker ground, crisper building outline). Wiring (store/service/migration 335) reviewed sound. See [[ADR-Location-Tab-Isometric-3D-Overhaul]].
- [[Build-Mode-Minecraft-Style-Placement]] — 2026-06-10 — Location tab 3D editor: ghost preview w/ grid snap + R/Q/E pre-rotation, drag-to-paint (camera pauses via `CameraRig controlsEnabled`), face-normal stacking/adjoining (`build-mode.ts`, 11 tests), ⌥click delete, middle-click pick-block, new structural kinds wall/platform/stairs/ramp. Shipped alongside [[Fix-Location-3D-Black-Scene-Night-Fog-SoftShadows]]. Chunk 467.7 KB (< 500).
- [[Scene3D-Catalog-Expansion-And-Perf]] — 2026-06-10 — Location tab 3D follow-up: legend hidden in 3D edit (was covering the library), toolbar zoom/fit now 3D-aware + NEW true fullscreen toggle (shell `requestFullscreen`), +27 catalog kinds (~45 total, `recipes-extra.tsx`) + library search, render-on-demand frameloop (idle GPU ~0, fly mode stays continuous) with invalidate plumbing, idle prefetch of the 3D chunk chain, `?showcase` harness grid. Chunk 489.6/500 KB — next batch must split.
- [[Rack-System-Builder-3D]] — 2026-06-10 — Full racking systems in the 3D editor: `rack-system.ts` generator (runs lettered A/B/C, back-to-back pairs + flue, aisle width; 8 tests), Racking library section + presets + RackSystemDialog (live footprint/locations summary), one-click placement of all runs as a single undo command, pallet-rack orange load beams + intermediate upright frames. manualChunks gotcha fixed (scene3d match was defeating React.lazy); chunk 479.4 KB.

- [[Floor-Plan-Envelope-3D]] — 2026-06-10 — Location tab: max facility footprint plotted as a `canvas_settings.floor_plan` JSONB envelope (no migration) — dashed boundary + dimension labels in-scene, computeBounds/camera coverage, red ghost + blocked placements outside a locked envelope, FloorPlanDialog (m/ft, ceiling→wall_height, save + draw building outline). 17 new tests; dialog excluded from feature-warehouse-3d chunk.
- [[Pick-Scenario-Simulation-3D]] — 2026-06-10 — Live pick-path scenarios in the 3D scene: virtual pickers walk real `get_pick_tour` A* tours with walking/picking state machine (frame-rate-independent sim-core, 13 tests), live KPIs (picks/labour-hr, m/pick), per-1m-cell congestion InstancedMesh heatmap, 1-10× time scale, run-comparison history. Entire `scene3d/simulation/` excluded from the feature chunk (sits at the 500 KB gate) — ships as its own lazy chunks. feature-warehouse-3d after both features: 483.8 KB.

- [[Rack-And-Object-Design-Controls-3D]] — 2026-06-11 — Location tab follow-up: racks fully configurable (level height drives total build height, post/shelf/beam colors, beams toggle via `metadata.appearance` + new lazy RackConfigPanel3D with undoable edits/duplicate/guarded delete), every scene object gets finish presets (matte/brushed/chrome/glossy) + neon glow via material traversal (`metadata.style`), floor-plan envelope now drag-movable + corner-resizable in-scene (border band + handles, grid snap, undo). 21 new tests (84 scene3d total); chunk 491.3/500 KB — NEXT in-chunk addition must split.

- [[Vehicle-Fleet-And-Catalog-Expansion-2]] — 2026-06-11 — Brand-spec parametric forklift fleet from researched real dimensions (Raymond 7500 reach + 9800 turret in Raymond red; Crown SP 3500 order picker + RC 5500 stand-up + PE rider in Crown beige; walkie stacker, tugger) + 12 facility extras (semi trailer, container, IBC, battery rack, dock leveler…) + new "Vehicles & Fleet" category. Unblocked by splitting all recipe geometry into feature-warehouse-recipes (~36 KB parallel chunk) — feature-warehouse-3d back to 479.6 KB.

- [[Facility-Layout-Templates]] — 2026-06-11 — Save any layout as a named facility template (kind, description, stats) and stamp out new facilities from the library: migration 336 `warehouse_layout_templates` (applied to prod via MCP), pure snapshot core with id-remap refs (6 tests), createFacility remaps zones/racks/objects/aisle graph into a fresh warehouse_maps row (bins never copied), Facilities & Templates dialog in the toolbar More menu + No-Map empty state, and map codes merged into the LX03-only warehouse picker via new `listMaps()`.
